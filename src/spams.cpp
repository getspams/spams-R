/* ----------------------------------------------------------------------------
 * This file was automatically generated by SWIG (http://www.swig.org).
 * Version 3.0.12
 *
 * This file is not intended to be easily readable and contains a number of
 * coding conventions designed to improve portability and efficiency. Do not make
 * changes to this file unless you know what you are doing--modify the SWIG
 * interface file instead.
 * ----------------------------------------------------------------------------- */


#ifndef SWIGR
#define SWIGR
#endif


#ifdef __cplusplus
/* SwigValueWrapper is described in swig.swg */
template<typename T> class SwigValueWrapper {
  struct SwigMovePointer {
    T *ptr;
    SwigMovePointer(T *p) : ptr(p) { }
    ~SwigMovePointer() { delete ptr; }
    SwigMovePointer& operator=(SwigMovePointer& rhs) { T* oldptr = ptr; ptr = 0; delete oldptr; ptr = rhs.ptr; rhs.ptr = 0; return *this; }
  } pointer;
  SwigValueWrapper& operator=(const SwigValueWrapper<T>& rhs);
  SwigValueWrapper(const SwigValueWrapper<T>& rhs);
public:
  SwigValueWrapper() : pointer(0) { }
  SwigValueWrapper& operator=(const T& t) { SwigMovePointer tmp(new T(t)); pointer = tmp; return *this; }
  operator T&() const { return *pointer.ptr; }
  T *operator&() { return pointer.ptr; }
};

template <typename T> T SwigValueInit() {
  return T();
}
#endif

/* -----------------------------------------------------------------------------
 *  This section contains generic SWIG labels for method/variable
 *  declarations/attributes, and other compiler dependent labels.
 * ----------------------------------------------------------------------------- */

/* template workaround for compilers that cannot correctly implement the C++ standard */
#ifndef SWIGTEMPLATEDISAMBIGUATOR
# if defined(__SUNPRO_CC) && (__SUNPRO_CC <= 0x560)
#  define SWIGTEMPLATEDISAMBIGUATOR template
# elif defined(__HP_aCC)
/* Needed even with `aCC -AA' when `aCC -V' reports HP ANSI C++ B3910B A.03.55 */
/* If we find a maximum version that requires this, the test would be __HP_aCC <= 35500 for A.03.55 */
#  define SWIGTEMPLATEDISAMBIGUATOR template
# else
#  define SWIGTEMPLATEDISAMBIGUATOR
# endif
#endif

/* inline attribute */
#ifndef SWIGINLINE
# if defined(__cplusplus) || (defined(__GNUC__) && !defined(__STRICT_ANSI__))
#   define SWIGINLINE inline
# else
#   define SWIGINLINE
# endif
#endif

/* attribute recognised by some compilers to avoid 'unused' warnings */
#ifndef SWIGUNUSED
# if defined(__GNUC__)
#   if !(defined(__cplusplus)) || (__GNUC__ > 3 || (__GNUC__ == 3 && __GNUC_MINOR__ >= 4))
#     define SWIGUNUSED __attribute__ ((__unused__))
#   else
#     define SWIGUNUSED
#   endif
# elif defined(__ICC)
#   define SWIGUNUSED __attribute__ ((__unused__))
# else
#   define SWIGUNUSED
# endif
#endif

#ifndef SWIG_MSC_UNSUPPRESS_4505
# if defined(_MSC_VER)
#   pragma warning(disable : 4505) /* unreferenced local function has been removed */
# endif
#endif

#ifndef SWIGUNUSEDPARM
# ifdef __cplusplus
#   define SWIGUNUSEDPARM(p)
# else
#   define SWIGUNUSEDPARM(p) p SWIGUNUSED
# endif
#endif

/* internal SWIG method */
#ifndef SWIGINTERN
# define SWIGINTERN static SWIGUNUSED
#endif

/* internal inline SWIG method */
#ifndef SWIGINTERNINLINE
# define SWIGINTERNINLINE SWIGINTERN SWIGINLINE
#endif

/* exporting methods */
#if defined(__GNUC__)
#  if (__GNUC__ >= 4) || (__GNUC__ == 3 && __GNUC_MINOR__ >= 4)
#    ifndef GCC_HASCLASSVISIBILITY
#      define GCC_HASCLASSVISIBILITY
#    endif
#  endif
#endif

#ifndef SWIGEXPORT
# if defined(_WIN32) || defined(__WIN32__) || defined(__CYGWIN__)
#   if defined(STATIC_LINKED)
#     define SWIGEXPORT
#   else
#     define SWIGEXPORT __declspec(dllexport)
#   endif
# else
#   if defined(__GNUC__) && defined(GCC_HASCLASSVISIBILITY)
#     define SWIGEXPORT __attribute__ ((visibility("default")))
#   else
#     define SWIGEXPORT
#   endif
# endif
#endif

/* calling conventions for Windows */
#ifndef SWIGSTDCALL
# if defined(_WIN32) || defined(__WIN32__) || defined(__CYGWIN__)
#   define SWIGSTDCALL __stdcall
# else
#   define SWIGSTDCALL
# endif
#endif

/* Deal with Microsoft's attempt at deprecating C standard runtime functions */
#if !defined(SWIG_NO_CRT_SECURE_NO_DEPRECATE) && defined(_MSC_VER) && !defined(_CRT_SECURE_NO_DEPRECATE)
# define _CRT_SECURE_NO_DEPRECATE
#endif

/* Deal with Microsoft's attempt at deprecating methods in the standard C++ library */
#if !defined(SWIG_NO_SCL_SECURE_NO_DEPRECATE) && defined(_MSC_VER) && !defined(_SCL_SECURE_NO_DEPRECATE)
# define _SCL_SECURE_NO_DEPRECATE
#endif

/* Deal with Apple's deprecated 'AssertMacros.h' from Carbon-framework */
#if defined(__APPLE__) && !defined(__ASSERT_MACROS_DEFINE_VERSIONS_WITHOUT_UNDERSCORES)
# define __ASSERT_MACROS_DEFINE_VERSIONS_WITHOUT_UNDERSCORES 0
#endif

/* Intel's compiler complains if a variable which was never initialised is
 * cast to void, which is a common idiom which we use to indicate that we
 * are aware a variable isn't used.  So we just silence that warning.
 * See: https://github.com/swig/swig/issues/192 for more discussion.
 */
#ifdef __INTEL_COMPILER
# pragma warning disable 592
#endif

/* -----------------------------------------------------------------------------
 * swigrun.swg
 *
 * This file contains generic C API SWIG runtime support for pointer
 * type checking.
 * ----------------------------------------------------------------------------- */

/* This should only be incremented when either the layout of swig_type_info changes,
   or for whatever reason, the runtime changes incompatibly */
#define SWIG_RUNTIME_VERSION "4"

/* define SWIG_TYPE_TABLE_NAME as "SWIG_TYPE_TABLE" */
#ifdef SWIG_TYPE_TABLE
# define SWIG_QUOTE_STRING(x) #x
# define SWIG_EXPAND_AND_QUOTE_STRING(x) SWIG_QUOTE_STRING(x)
# define SWIG_TYPE_TABLE_NAME SWIG_EXPAND_AND_QUOTE_STRING(SWIG_TYPE_TABLE)
#else
# define SWIG_TYPE_TABLE_NAME
#endif

/*
  You can use the SWIGRUNTIME and SWIGRUNTIMEINLINE macros for
  creating a static or dynamic library from the SWIG runtime code.
  In 99.9% of the cases, SWIG just needs to declare them as 'static'.

  But only do this if strictly necessary, ie, if you have problems
  with your compiler or suchlike.
*/

#ifndef SWIGRUNTIME
# define SWIGRUNTIME SWIGINTERN
#endif

#ifndef SWIGRUNTIMEINLINE
# define SWIGRUNTIMEINLINE SWIGRUNTIME SWIGINLINE
#endif

/*  Generic buffer size */
#ifndef SWIG_BUFFER_SIZE
# define SWIG_BUFFER_SIZE 1024
#endif

/* Flags for pointer conversions */
#define SWIG_POINTER_DISOWN        0x1
#define SWIG_CAST_NEW_MEMORY       0x2

/* Flags for new pointer objects */
#define SWIG_POINTER_OWN           0x1


/*
   Flags/methods for returning states.

   The SWIG conversion methods, as ConvertPtr, return an integer
   that tells if the conversion was successful or not. And if not,
   an error code can be returned (see swigerrors.swg for the codes).

   Use the following macros/flags to set or process the returning
   states.

   In old versions of SWIG, code such as the following was usually written:

     if (SWIG_ConvertPtr(obj,vptr,ty.flags) != -1) {
       // success code
     } else {
       //fail code
     }

   Now you can be more explicit:

    int res = SWIG_ConvertPtr(obj,vptr,ty.flags);
    if (SWIG_IsOK(res)) {
      // success code
    } else {
      // fail code
    }

   which is the same really, but now you can also do

    Type *ptr;
    int res = SWIG_ConvertPtr(obj,(void **)(&ptr),ty.flags);
    if (SWIG_IsOK(res)) {
      // success code
      if (SWIG_IsNewObj(res) {
        ...
	delete *ptr;
      } else {
        ...
      }
    } else {
      // fail code
    }

   I.e., now SWIG_ConvertPtr can return new objects and you can
   identify the case and take care of the deallocation. Of course that
   also requires SWIG_ConvertPtr to return new result values, such as

      int SWIG_ConvertPtr(obj, ptr,...) {
        if (<obj is ok>) {
          if (<need new object>) {
            *ptr = <ptr to new allocated object>;
            return SWIG_NEWOBJ;
          } else {
            *ptr = <ptr to old object>;
            return SWIG_OLDOBJ;
          }
        } else {
          return SWIG_BADOBJ;
        }
      }

   Of course, returning the plain '0(success)/-1(fail)' still works, but you can be
   more explicit by returning SWIG_BADOBJ, SWIG_ERROR or any of the
   SWIG errors code.

   Finally, if the SWIG_CASTRANK_MODE is enabled, the result code
   allows to return the 'cast rank', for example, if you have this

       int food(double)
       int fooi(int);

   and you call

      food(1)   // cast rank '1'  (1 -> 1.0)
      fooi(1)   // cast rank '0'

   just use the SWIG_AddCast()/SWIG_CheckState()
*/

#define SWIG_OK                    (0)
#define SWIG_ERROR                 (-1)
#define SWIG_IsOK(r)               (r >= 0)
#define SWIG_ArgError(r)           ((r != SWIG_ERROR) ? r : SWIG_TypeError)

/* The CastRankLimit says how many bits are used for the cast rank */
#define SWIG_CASTRANKLIMIT         (1 << 8)
/* The NewMask denotes the object was created (using new/malloc) */
#define SWIG_NEWOBJMASK            (SWIG_CASTRANKLIMIT  << 1)
/* The TmpMask is for in/out typemaps that use temporal objects */
#define SWIG_TMPOBJMASK            (SWIG_NEWOBJMASK << 1)
/* Simple returning values */
#define SWIG_BADOBJ                (SWIG_ERROR)
#define SWIG_OLDOBJ                (SWIG_OK)
#define SWIG_NEWOBJ                (SWIG_OK | SWIG_NEWOBJMASK)
#define SWIG_TMPOBJ                (SWIG_OK | SWIG_TMPOBJMASK)
/* Check, add and del mask methods */
#define SWIG_AddNewMask(r)         (SWIG_IsOK(r) ? (r | SWIG_NEWOBJMASK) : r)
#define SWIG_DelNewMask(r)         (SWIG_IsOK(r) ? (r & ~SWIG_NEWOBJMASK) : r)
#define SWIG_IsNewObj(r)           (SWIG_IsOK(r) && (r & SWIG_NEWOBJMASK))
#define SWIG_AddTmpMask(r)         (SWIG_IsOK(r) ? (r | SWIG_TMPOBJMASK) : r)
#define SWIG_DelTmpMask(r)         (SWIG_IsOK(r) ? (r & ~SWIG_TMPOBJMASK) : r)
#define SWIG_IsTmpObj(r)           (SWIG_IsOK(r) && (r & SWIG_TMPOBJMASK))

/* Cast-Rank Mode */
#if defined(SWIG_CASTRANK_MODE)
#  ifndef SWIG_TypeRank
#    define SWIG_TypeRank             unsigned long
#  endif
#  ifndef SWIG_MAXCASTRANK            /* Default cast allowed */
#    define SWIG_MAXCASTRANK          (2)
#  endif
#  define SWIG_CASTRANKMASK          ((SWIG_CASTRANKLIMIT) -1)
#  define SWIG_CastRank(r)           (r & SWIG_CASTRANKMASK)
SWIGINTERNINLINE int SWIG_AddCast(int r) {
  return SWIG_IsOK(r) ? ((SWIG_CastRank(r) < SWIG_MAXCASTRANK) ? (r + 1) : SWIG_ERROR) : r;
}
SWIGINTERNINLINE int SWIG_CheckState(int r) {
  return SWIG_IsOK(r) ? SWIG_CastRank(r) + 1 : 0;
}
#else /* no cast-rank mode */
#  define SWIG_AddCast(r) (r)
#  define SWIG_CheckState(r) (SWIG_IsOK(r) ? 1 : 0)
#endif


#include <string.h>

#ifdef __cplusplus
extern "C" {
#endif

typedef void *(*swig_converter_func)(void *, int *);
typedef struct swig_type_info *(*swig_dycast_func)(void **);

/* Structure to store information on one type */
typedef struct swig_type_info {
  const char             *name;			/* mangled name of this type */
  const char             *str;			/* human readable name of this type */
  swig_dycast_func        dcast;		/* dynamic cast function down a hierarchy */
  struct swig_cast_info  *cast;			/* linked list of types that can cast into this type */
  void                   *clientdata;		/* language specific type data */
  int                    owndata;		/* flag if the structure owns the clientdata */
} swig_type_info;

/* Structure to store a type and conversion function used for casting */
typedef struct swig_cast_info {
  swig_type_info         *type;			/* pointer to type that is equivalent to this type */
  swig_converter_func     converter;		/* function to cast the void pointers */
  struct swig_cast_info  *next;			/* pointer to next cast in linked list */
  struct swig_cast_info  *prev;			/* pointer to the previous cast */
} swig_cast_info;

/* Structure used to store module information
 * Each module generates one structure like this, and the runtime collects
 * all of these structures and stores them in a circularly linked list.*/
typedef struct swig_module_info {
  swig_type_info         **types;		/* Array of pointers to swig_type_info structures that are in this module */
  size_t                 size;		        /* Number of types in this module */
  struct swig_module_info *next;		/* Pointer to next element in circularly linked list */
  swig_type_info         **type_initial;	/* Array of initially generated type structures */
  swig_cast_info         **cast_initial;	/* Array of initially generated casting structures */
  void                    *clientdata;		/* Language specific module data */
} swig_module_info;

/*
  Compare two type names skipping the space characters, therefore
  "char*" == "char *" and "Class<int>" == "Class<int >", etc.

  Return 0 when the two name types are equivalent, as in
  strncmp, but skipping ' '.
*/
SWIGRUNTIME int
SWIG_TypeNameComp(const char *f1, const char *l1,
		  const char *f2, const char *l2) {
  for (;(f1 != l1) && (f2 != l2); ++f1, ++f2) {
    while ((*f1 == ' ') && (f1 != l1)) ++f1;
    while ((*f2 == ' ') && (f2 != l2)) ++f2;
    if (*f1 != *f2) return (*f1 > *f2) ? 1 : -1;
  }
  return (int)((l1 - f1) - (l2 - f2));
}

/*
  Check type equivalence in a name list like <name1>|<name2>|...
  Return 0 if equal, -1 if nb < tb, 1 if nb > tb
*/
SWIGRUNTIME int
SWIG_TypeCmp(const char *nb, const char *tb) {
  int equiv = 1;
  const char* te = tb + strlen(tb);
  const char* ne = nb;
  while (equiv != 0 && *ne) {
    for (nb = ne; *ne; ++ne) {
      if (*ne == '|') break;
    }
    equiv = SWIG_TypeNameComp(nb, ne, tb, te);
    if (*ne) ++ne;
  }
  return equiv;
}

/*
  Check type equivalence in a name list like <name1>|<name2>|...
  Return 0 if not equal, 1 if equal
*/
SWIGRUNTIME int
SWIG_TypeEquiv(const char *nb, const char *tb) {
  return SWIG_TypeCmp(nb, tb) == 0 ? 1 : 0;
}

/*
  Check the typename
*/
SWIGRUNTIME swig_cast_info *
SWIG_TypeCheck(const char *c, swig_type_info *ty) {
  if (ty) {
    swig_cast_info *iter = ty->cast;
    while (iter) {
      if (strcmp(iter->type->name, c) == 0) {
        if (iter == ty->cast)
          return iter;
        /* Move iter to the top of the linked list */
        iter->prev->next = iter->next;
        if (iter->next)
          iter->next->prev = iter->prev;
        iter->next = ty->cast;
        iter->prev = 0;
        if (ty->cast) ty->cast->prev = iter;
        ty->cast = iter;
        return iter;
      }
      iter = iter->next;
    }
  }
  return 0;
}

/*
  Identical to SWIG_TypeCheck, except strcmp is replaced with a pointer comparison
*/
SWIGRUNTIME swig_cast_info *
SWIG_TypeCheckStruct(swig_type_info *from, swig_type_info *ty) {
  if (ty) {
    swig_cast_info *iter = ty->cast;
    while (iter) {
      if (iter->type == from) {
        if (iter == ty->cast)
          return iter;
        /* Move iter to the top of the linked list */
        iter->prev->next = iter->next;
        if (iter->next)
          iter->next->prev = iter->prev;
        iter->next = ty->cast;
        iter->prev = 0;
        if (ty->cast) ty->cast->prev = iter;
        ty->cast = iter;
        return iter;
      }
      iter = iter->next;
    }
  }
  return 0;
}

/*
  Cast a pointer up an inheritance hierarchy
*/
SWIGRUNTIMEINLINE void *
SWIG_TypeCast(swig_cast_info *ty, void *ptr, int *newmemory) {
  return ((!ty) || (!ty->converter)) ? ptr : (*ty->converter)(ptr, newmemory);
}

/*
   Dynamic pointer casting. Down an inheritance hierarchy
*/
SWIGRUNTIME swig_type_info *
SWIG_TypeDynamicCast(swig_type_info *ty, void **ptr) {
  swig_type_info *lastty = ty;
  if (!ty || !ty->dcast) return ty;
  while (ty && (ty->dcast)) {
    ty = (*ty->dcast)(ptr);
    if (ty) lastty = ty;
  }
  return lastty;
}

/*
  Return the name associated with this type
*/
SWIGRUNTIMEINLINE const char *
SWIG_TypeName(const swig_type_info *ty) {
  return ty->name;
}

/*
  Return the pretty name associated with this type,
  that is an unmangled type name in a form presentable to the user.
*/
SWIGRUNTIME const char *
SWIG_TypePrettyName(const swig_type_info *type) {
  /* The "str" field contains the equivalent pretty names of the
     type, separated by vertical-bar characters.  We choose
     to print the last name, as it is often (?) the most
     specific. */
  if (!type) return NULL;
  if (type->str != NULL) {
    const char *last_name = type->str;
    const char *s;
    for (s = type->str; *s; s++)
      if (*s == '|') last_name = s+1;
    return last_name;
  }
  else
    return type->name;
}

/*
   Set the clientdata field for a type
*/
SWIGRUNTIME void
SWIG_TypeClientData(swig_type_info *ti, void *clientdata) {
  swig_cast_info *cast = ti->cast;
  /* if (ti->clientdata == clientdata) return; */
  ti->clientdata = clientdata;

  while (cast) {
    if (!cast->converter) {
      swig_type_info *tc = cast->type;
      if (!tc->clientdata) {
	SWIG_TypeClientData(tc, clientdata);
      }
    }
    cast = cast->next;
  }
}
SWIGRUNTIME void
SWIG_TypeNewClientData(swig_type_info *ti, void *clientdata) {
  SWIG_TypeClientData(ti, clientdata);
  ti->owndata = 1;
}

/*
  Search for a swig_type_info structure only by mangled name
  Search is a O(log #types)

  We start searching at module start, and finish searching when start == end.
  Note: if start == end at the beginning of the function, we go all the way around
  the circular list.
*/
SWIGRUNTIME swig_type_info *
SWIG_MangledTypeQueryModule(swig_module_info *start,
                            swig_module_info *end,
		            const char *name) {
  swig_module_info *iter = start;
  do {
    if (iter->size) {
      size_t l = 0;
      size_t r = iter->size - 1;
      do {
	/* since l+r >= 0, we can (>> 1) instead (/ 2) */
	size_t i = (l + r) >> 1;
	const char *iname = iter->types[i]->name;
	if (iname) {
	  int compare = strcmp(name, iname);
	  if (compare == 0) {
	    return iter->types[i];
	  } else if (compare < 0) {
	    if (i) {
	      r = i - 1;
	    } else {
	      break;
	    }
	  } else if (compare > 0) {
	    l = i + 1;
	  }
	} else {
	  break; /* should never happen */
	}
      } while (l <= r);
    }
    iter = iter->next;
  } while (iter != end);
  return 0;
}

/*
  Search for a swig_type_info structure for either a mangled name or a human readable name.
  It first searches the mangled names of the types, which is a O(log #types)
  If a type is not found it then searches the human readable names, which is O(#types).

  We start searching at module start, and finish searching when start == end.
  Note: if start == end at the beginning of the function, we go all the way around
  the circular list.
*/
SWIGRUNTIME swig_type_info *
SWIG_TypeQueryModule(swig_module_info *start,
                     swig_module_info *end,
		     const char *name) {
  /* STEP 1: Search the name field using binary search */
  swig_type_info *ret = SWIG_MangledTypeQueryModule(start, end, name);
  if (ret) {
    return ret;
  } else {
    /* STEP 2: If the type hasn't been found, do a complete search
       of the str field (the human readable name) */
    swig_module_info *iter = start;
    do {
      size_t i = 0;
      for (; i < iter->size; ++i) {
	if (iter->types[i]->str && (SWIG_TypeEquiv(iter->types[i]->str, name)))
	  return iter->types[i];
      }
      iter = iter->next;
    } while (iter != end);
  }

  /* neither found a match */
  return 0;
}

/*
   Pack binary data into a string
*/
SWIGRUNTIME char *
SWIG_PackData(char *c, void *ptr, size_t sz) {
  static const char hex[17] = "0123456789abcdef";
  const unsigned char *u = (unsigned char *) ptr;
  const unsigned char *eu =  u + sz;
  for (; u != eu; ++u) {
    unsigned char uu = *u;
    *(c++) = hex[(uu & 0xf0) >> 4];
    *(c++) = hex[uu & 0xf];
  }
  return c;
}

/*
   Unpack binary data from a string
*/
SWIGRUNTIME const char *
SWIG_UnpackData(const char *c, void *ptr, size_t sz) {
  unsigned char *u = (unsigned char *) ptr;
  const unsigned char *eu = u + sz;
  for (; u != eu; ++u) {
    char d = *(c++);
    unsigned char uu;
    if ((d >= '0') && (d <= '9'))
      uu = (unsigned char)((d - '0') << 4);
    else if ((d >= 'a') && (d <= 'f'))
      uu = (unsigned char)((d - ('a'-10)) << 4);
    else
      return (char *) 0;
    d = *(c++);
    if ((d >= '0') && (d <= '9'))
      uu |= (unsigned char)(d - '0');
    else if ((d >= 'a') && (d <= 'f'))
      uu |= (unsigned char)(d - ('a'-10));
    else
      return (char *) 0;
    *u = uu;
  }
  return c;
}

/*
   Pack 'void *' into a string buffer.
*/
SWIGRUNTIME char *
SWIG_PackVoidPtr(char *buff, void *ptr, const char *name, size_t bsz) {
  char *r = buff;
  if ((2*sizeof(void *) + 2) > bsz) return 0;
  *(r++) = '_';
  r = SWIG_PackData(r,&ptr,sizeof(void *));
  if (strlen(name) + 1 > (bsz - (r - buff))) return 0;
  strcpy(r,name);
  return buff;
}

SWIGRUNTIME const char *
SWIG_UnpackVoidPtr(const char *c, void **ptr, const char *name) {
  if (*c != '_') {
    if (strcmp(c,"NULL") == 0) {
      *ptr = (void *) 0;
      return name;
    } else {
      return 0;
    }
  }
  return SWIG_UnpackData(++c,ptr,sizeof(void *));
}

SWIGRUNTIME char *
SWIG_PackDataName(char *buff, void *ptr, size_t sz, const char *name, size_t bsz) {
  char *r = buff;
  size_t lname = (name ? strlen(name) : 0);
  if ((2*sz + 2 + lname) > bsz) return 0;
  *(r++) = '_';
  r = SWIG_PackData(r,ptr,sz);
  if (lname) {
    strncpy(r,name,lname+1);
  } else {
    *r = 0;
  }
  return buff;
}

SWIGRUNTIME const char *
SWIG_UnpackDataName(const char *c, void *ptr, size_t sz, const char *name) {
  if (*c != '_') {
    if (strcmp(c,"NULL") == 0) {
      memset(ptr,0,sz);
      return name;
    } else {
      return 0;
    }
  }
  return SWIG_UnpackData(++c,ptr,sz);
}

#ifdef __cplusplus
}
#endif


/* Remove global namespace pollution */
#if !defined(SWIG_NO_R_NO_REMAP)
# define R_NO_REMAP
#endif
#if !defined(SWIG_NO_STRICT_R_HEADERS)
# define STRICT_R_HEADERS
#endif

#include <Rdefines.h>
#include <Rversion.h>

#ifdef __cplusplus
#include <exception>
extern "C" {
#endif

/* for raw pointer */
#define SWIG_ConvertPtr(obj, pptr, type, flags)         SWIG_R_ConvertPtr(obj, pptr, type, flags)
#define SWIG_ConvertPtrAndOwn(obj,pptr,type,flags,own)  SWIG_R_ConvertPtr(obj, pptr, type, flags)
#define SWIG_NewPointerObj(ptr, type, flags)            SWIG_R_NewPointerObj(ptr, type, flags)

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>

#if R_VERSION >= R_Version(2,6,0)
#define VMAXTYPE void *
#else
#define VMAXTYPE char *
#endif

/*
  This is mainly a way to avoid having lots of local variables that may 
  conflict with those in the routine.

   Change name to R_SWIG_Callb....
*/
typedef struct RCallbackFunctionData {

  SEXP fun;
  SEXP userData;


  SEXP expr;
  SEXP retValue;
  int errorOccurred;

  SEXP el;  /* Temporary pointer used in the construction of the expression to call the R function. */

  struct RCallbackFunctionData *previous;   /* Stack */

} RCallbackFunctionData;

static RCallbackFunctionData  *callbackFunctionDataStack;


SWIGRUNTIME SEXP
R_SWIG_debug_getCallbackFunctionData()
{
  int n, i;
  SEXP ans;
  RCallbackFunctionData  *p = callbackFunctionDataStack;

  n = 0;
  while(p) { 
    n++;
    p = p->previous;
  }

  Rf_protect(ans = Rf_allocVector(VECSXP, n));
  for(p = callbackFunctionDataStack, i = 0; i < n; p = p->previous, i++) 
      SET_VECTOR_ELT(ans, i, p->fun);

  Rf_unprotect(1);

  return(ans);
}



SWIGRUNTIME RCallbackFunctionData *
R_SWIG_pushCallbackFunctionData(SEXP fun, SEXP userData)
{
   RCallbackFunctionData *el;
   el = (RCallbackFunctionData *) calloc(1, sizeof(RCallbackFunctionData));
   el->fun = fun;
   el->userData = userData;
   el->previous = callbackFunctionDataStack;

   callbackFunctionDataStack = el;

   return(el);
}


SWIGRUNTIME SEXP
R_SWIG_R_pushCallbackFunctionData(SEXP fun, SEXP userData)
{
    R_SWIG_pushCallbackFunctionData(fun, userData);
    return R_NilValue;
}

SWIGRUNTIME RCallbackFunctionData *
R_SWIG_getCallbackFunctionData()
{
  if(!callbackFunctionDataStack) {
    Rf_error("Supposedly impossible error occurred in the SWIG callback mechanism."
            "  No callback function data set.");
  }
  
  return callbackFunctionDataStack;
}

SWIGRUNTIME void
R_SWIG_popCallbackFunctionData(int doFree)
{
  RCallbackFunctionData  *el = NULL;
  if(!callbackFunctionDataStack)
    return ; /* Error !!! */

  el = callbackFunctionDataStack ;
  callbackFunctionDataStack = callbackFunctionDataStack->previous;

  if(doFree)
     free(el);
}


/*
  Interface to S function
      is(obj, type)
  which is to be used to determine if an 
  external pointer inherits from the right class.

  Ideally, we would like to be able to do this without an explicit call to the is() function.
  When the S4 class system uses its own SEXP types, then we will hopefully be able to do this
  in the C code.

  Should we make the expression static and preserve it to avoid the overhead of 
  allocating each time.
*/
SWIGRUNTIME int
R_SWIG_checkInherits(SEXP obj, SEXP tag, const char *type)
{
  SEXP e, val;
  int check_err = 0;

  Rf_protect(e = Rf_allocVector(LANGSXP, 3));
  SETCAR(e, Rf_install("extends"));

  SETCAR(CDR(e), Rf_mkString(CHAR(PRINTNAME(tag))));
  SETCAR(CDR(CDR(e)), Rf_mkString(type));

  val = R_tryEval(e, R_GlobalEnv, &check_err);
  Rf_unprotect(1);
  if(check_err) 
    return(0);


  return(LOGICAL(val)[0]);
}


SWIGRUNTIME void *
R_SWIG_resolveExternalRef(SEXP arg, const char * const type, const char * const argName, Rboolean nullOk)
{
  void *ptr;
  SEXP orig = arg;

  if(TYPEOF(arg) != EXTPTRSXP) 
    arg = GET_SLOT(arg, Rf_mkString("ref"));

  
  if(TYPEOF(arg) != EXTPTRSXP) {
    Rf_error("argument %s must be an external pointer (from an ExternalReference)", argName);
  }


  ptr = R_ExternalPtrAddr(arg);

  if(ptr == NULL && nullOk == (Rboolean) FALSE) {
    Rf_error("the external pointer (of type %s) for argument %s has value NULL", argName, type);
  }

  if(type[0] && R_ExternalPtrTag(arg) != Rf_install(type) && strcmp(type, "voidRef")
      && !R_SWIG_checkInherits(orig,  R_ExternalPtrTag(arg), type)) {
    Rf_error("the external pointer for argument %s has tag %s, not the expected value %s",
             argName, CHAR(PRINTNAME(R_ExternalPtrTag(arg))), type);
  }


  return(ptr);
}

SWIGRUNTIME void
R_SWIG_ReferenceFinalizer(SEXP el)
{
  void *ptr = R_SWIG_resolveExternalRef(el, "", "<finalizer>",  (Rboolean) 1);
  fprintf(stderr, "In R_SWIG_ReferenceFinalizer for %p\n", ptr);
  Rf_PrintValue(el);

  if(ptr) {
     if(TYPEOF(el) != EXTPTRSXP)
        el = GET_SLOT(el, Rf_mkString("ref"));

     if(TYPEOF(el) == EXTPTRSXP)
        R_ClearExternalPtr(el);

     free(ptr);
  }

  return;
}

typedef enum {R_SWIG_EXTERNAL, R_SWIG_OWNER } R_SWIG_Owner;

SWIGRUNTIME SEXP
SWIG_MakePtr(void *ptr, const char *typeName, R_SWIG_Owner owner)
{
  SEXP external, r_obj;

  Rf_protect(external = R_MakeExternalPtr(ptr, Rf_install(typeName), R_NilValue));
  Rf_protect(r_obj = NEW_OBJECT(MAKE_CLASS((char *) typeName)));

  if(owner)
    R_RegisterCFinalizer(external, R_SWIG_ReferenceFinalizer);

  r_obj = SET_SLOT(r_obj, Rf_mkString((char *) "ref"), external);
  SET_S4_OBJECT(r_obj);
  Rf_unprotect(2);

  return(r_obj);
}


SWIGRUNTIME SEXP
R_SWIG_create_SWIG_R_Array(const char *typeName, SEXP ref, int len)
{
   SEXP arr;

/*XXX remove the char * cast when we can. MAKE_CLASS should be declared appropriately. */
   Rf_protect(arr = NEW_OBJECT(MAKE_CLASS((char *) typeName)));
   Rf_protect(arr = R_do_slot_assign(arr, Rf_mkString("ref"), ref));
   Rf_protect(arr = R_do_slot_assign(arr, Rf_mkString("dims"), Rf_ScalarInteger(len)));

   Rf_unprotect(3); 			   
   SET_S4_OBJECT(arr);	
   return arr;
}

#define ADD_OUTPUT_ARG(result, pos, value, name)  r_ans = AddOutputArgToReturn(pos, value, name, OutputValues);

SWIGRUNTIME SEXP
AddOutputArgToReturn(int pos, SEXP value, const char *name, SEXP output)
{
  SET_VECTOR_ELT(output, pos, value);

  return(output);
}

/* Create a new pointer object */
SWIGRUNTIMEINLINE SEXP
SWIG_R_NewPointerObj(void *ptr, swig_type_info *type, int flags) {
  SEXP rptr = R_MakeExternalPtr(ptr, 
  R_MakeExternalPtr(type, R_NilValue, R_NilValue), R_NilValue); 
  SET_S4_OBJECT(rptr);
  return rptr;
}


/* Convert a pointer value */
SWIGRUNTIMEINLINE int
SWIG_R_ConvertPtr(SEXP obj, void **ptr, swig_type_info *ty, int flags) {
  void *vptr;
  if (!obj) return SWIG_ERROR;
  if (obj == R_NilValue) {
    if (ptr) *ptr = NULL;
    return SWIG_OK;
  }

  vptr = R_ExternalPtrAddr(obj);
  if (ty) {
    swig_type_info *to = (swig_type_info*) 
      R_ExternalPtrAddr(R_ExternalPtrTag(obj));
    if (to == ty) {
      if (ptr) *ptr = vptr;
    } else {
      swig_cast_info *tc = SWIG_TypeCheck(to->name,ty);
      int newmemory = 0;
      if (ptr) *ptr = SWIG_TypeCast(tc,vptr,&newmemory);
      assert(!newmemory); /* newmemory handling not yet implemented */
    }
  } else {
      if (ptr) *ptr = vptr;
 }
  return SWIG_OK;
}

SWIGRUNTIME swig_module_info *
SWIG_GetModule(void *SWIGUNUSEDPARM(clientdata)) {
  static void *type_pointer = (void *)0;
  return (swig_module_info *) type_pointer;
}

SWIGRUNTIME void
SWIG_SetModule(void *v, swig_module_info *swig_module) {
}

typedef struct {
  void *pack;
  swig_type_info *ty;
  size_t size;
} RSwigPacked;

/* Create a new packed object */

SWIGRUNTIMEINLINE SEXP RSwigPacked_New(void *ptr, size_t sz,
		  swig_type_info *ty) {
  SEXP rptr;
  RSwigPacked *sobj = 
  (RSwigPacked*) malloc(sizeof(RSwigPacked));
  if (sobj) {
    void *pack = malloc(sz);
    if (pack) {
      memcpy(pack, ptr, sz);
      sobj->pack = pack;
      sobj->ty   = ty;
      sobj->size = sz;
    } else {
      sobj = 0;
    }
  }
  rptr = R_MakeExternalPtr(sobj, R_NilValue, R_NilValue); 
  return rptr;
}

SWIGRUNTIME swig_type_info *
RSwigPacked_UnpackData(SEXP obj, void *ptr, size_t size)
{
    RSwigPacked *sobj = 
        (RSwigPacked *)R_ExternalPtrAddr(obj);
    if (sobj->size != size) return 0;
    memcpy(ptr, sobj->pack, size);
    return sobj->ty;
}

SWIGRUNTIMEINLINE SEXP
SWIG_R_NewPackedObj(void *ptr, size_t sz, swig_type_info *type) {
  return ptr ? RSwigPacked_New((void *) ptr, sz, type) : R_NilValue;
}

/* Convert a packed value value */

SWIGRUNTIME int
SWIG_R_ConvertPacked(SEXP obj, void *ptr, size_t sz, swig_type_info *ty) {
  swig_type_info *to = RSwigPacked_UnpackData(obj, ptr, sz);
  if (!to) return SWIG_ERROR;
  if (ty) {
    if (to != ty) {
      /* check type cast? */
      swig_cast_info *tc = SWIG_TypeCheck(to->name,ty);
      if (!tc) return SWIG_ERROR;
    }
  }
  return SWIG_OK;
}  

#ifdef __cplusplus
#define SWIG_exception_noreturn(code, msg) do { throw std::runtime_error(msg); } while(0)
#else
#define SWIG_exception_noreturn(code, msg) do { return result; } while(0)
#endif

#ifdef __cplusplus
}
#endif



#define SWIG_exception_fail(code, msg) do { Rf_warning(msg); return Rf_ScalarLogical(NA_LOGICAL); return Rf_ScalarLogical(NA_LOGICAL); } while(0) 

#define SWIG_contract_assert(expr, msg) if (!(expr)) { Rf_warning(msg); return Rf_ScalarLogical(NA_LOGICAL); return Rf_ScalarLogical(NA_LOGICAL); } else 



  #define SWIG_exception(code, msg) do { switch (code) {case SWIG_IndexError: return Rf_ScalarLogical(NA_LOGICAL); default: Rf_warning(msg); return Rf_ScalarLogical(NA_LOGICAL); return Rf_ScalarLogical(NA_LOGICAL);}; } while(0) 



/* -------- TYPES TABLE (BEGIN) -------- */

#define SWIGTYPE_p_MatrixT_bool_t swig_types[0]
#define SWIGTYPE_p_char swig_types[1]
#define SWIGTYPE_p_int swig_types[2]
#define SWIGTYPE_p_p_SpMatrixT_bool_t swig_types[3]
#define SWIGTYPE_p_p_SpMatrixT_double_t swig_types[4]
#define SWIGTYPE_p_p_int swig_types[5]
#define SWIGTYPE_p_std__vectorT_int_t swig_types[6]
#define SWIGTYPE_p_std__vectorT_std__string_t swig_types[7]
#define SWIGTYPE_p_string swig_types[8]
static swig_type_info *swig_types[10];
static swig_module_info swig_module = {swig_types, 9, 0, 0, 0, 0};
#define SWIG_TypeQuery(name) SWIG_TypeQueryModule(&swig_module, &swig_module, name)
#define SWIG_MangledTypeQuery(name) SWIG_MangledTypeQueryModule(&swig_module, &swig_module, name)

/* -------- TYPES TABLE (END) -------- */


/* -----------------------------------------------------------------------------
 *  This section contains generic SWIG labels for method/variable
 *  declarations/attributes, and other compiler dependent labels.
 * ----------------------------------------------------------------------------- */

/* template workaround for compilers that cannot correctly implement the C++ standard */
#ifndef SWIGTEMPLATEDISAMBIGUATOR
# if defined(__SUNPRO_CC) && (__SUNPRO_CC <= 0x560)
#  define SWIGTEMPLATEDISAMBIGUATOR template
# elif defined(__HP_aCC)
/* Needed even with `aCC -AA' when `aCC -V' reports HP ANSI C++ B3910B A.03.55 */
/* If we find a maximum version that requires this, the test would be __HP_aCC <= 35500 for A.03.55 */
#  define SWIGTEMPLATEDISAMBIGUATOR template
# else
#  define SWIGTEMPLATEDISAMBIGUATOR
# endif
#endif

/* inline attribute */
#ifndef SWIGINLINE
# if defined(__cplusplus) || (defined(__GNUC__) && !defined(__STRICT_ANSI__))
#   define SWIGINLINE inline
# else
#   define SWIGINLINE
# endif
#endif

/* attribute recognised by some compilers to avoid 'unused' warnings */
#ifndef SWIGUNUSED
# if defined(__GNUC__)
#   if !(defined(__cplusplus)) || (__GNUC__ > 3 || (__GNUC__ == 3 && __GNUC_MINOR__ >= 4))
#     define SWIGUNUSED __attribute__ ((__unused__))
#   else
#     define SWIGUNUSED
#   endif
# elif defined(__ICC)
#   define SWIGUNUSED __attribute__ ((__unused__))
# else
#   define SWIGUNUSED
# endif
#endif

#ifndef SWIG_MSC_UNSUPPRESS_4505
# if defined(_MSC_VER)
#   pragma warning(disable : 4505) /* unreferenced local function has been removed */
# endif
#endif

#ifndef SWIGUNUSEDPARM
# ifdef __cplusplus
#   define SWIGUNUSEDPARM(p)
# else
#   define SWIGUNUSEDPARM(p) p SWIGUNUSED
# endif
#endif

/* internal SWIG method */
#ifndef SWIGINTERN
# define SWIGINTERN static SWIGUNUSED
#endif

/* internal inline SWIG method */
#ifndef SWIGINTERNINLINE
# define SWIGINTERNINLINE SWIGINTERN SWIGINLINE
#endif

/* exporting methods */
#if defined(__GNUC__)
#  if (__GNUC__ >= 4) || (__GNUC__ == 3 && __GNUC_MINOR__ >= 4)
#    ifndef GCC_HASCLASSVISIBILITY
#      define GCC_HASCLASSVISIBILITY
#    endif
#  endif
#endif

#ifndef SWIGEXPORT
# if defined(_WIN32) || defined(__WIN32__) || defined(__CYGWIN__)
#   if defined(STATIC_LINKED)
#     define SWIGEXPORT
#   else
#     define SWIGEXPORT __declspec(dllexport)
#   endif
# else
#   if defined(__GNUC__) && defined(GCC_HASCLASSVISIBILITY)
#     define SWIGEXPORT __attribute__ ((visibility("default")))
#   else
#     define SWIGEXPORT
#   endif
# endif
#endif

/* calling conventions for Windows */
#ifndef SWIGSTDCALL
# if defined(_WIN32) || defined(__WIN32__) || defined(__CYGWIN__)
#   define SWIGSTDCALL __stdcall
# else
#   define SWIGSTDCALL
# endif
#endif

/* Deal with Microsoft's attempt at deprecating C standard runtime functions */
#if !defined(SWIG_NO_CRT_SECURE_NO_DEPRECATE) && defined(_MSC_VER) && !defined(_CRT_SECURE_NO_DEPRECATE)
# define _CRT_SECURE_NO_DEPRECATE
#endif

/* Deal with Microsoft's attempt at deprecating methods in the standard C++ library */
#if !defined(SWIG_NO_SCL_SECURE_NO_DEPRECATE) && defined(_MSC_VER) && !defined(_SCL_SECURE_NO_DEPRECATE)
# define _SCL_SECURE_NO_DEPRECATE
#endif

/* Deal with Apple's deprecated 'AssertMacros.h' from Carbon-framework */
#if defined(__APPLE__) && !defined(__ASSERT_MACROS_DEFINE_VERSIONS_WITHOUT_UNDERSCORES)
# define __ASSERT_MACROS_DEFINE_VERSIONS_WITHOUT_UNDERSCORES 0
#endif

/* Intel's compiler complains if a variable which was never initialised is
 * cast to void, which is a common idiom which we use to indicate that we
 * are aware a variable isn't used.  So we just silence that warning.
 * See: https://github.com/swig/swig/issues/192 for more discussion.
 */
#ifdef __INTEL_COMPILER
# pragma warning disable 592
#endif

/*  Errors in SWIG */
#define  SWIG_UnknownError    	   -1
#define  SWIG_IOError        	   -2
#define  SWIG_RuntimeError   	   -3
#define  SWIG_IndexError     	   -4
#define  SWIG_TypeError      	   -5
#define  SWIG_DivisionByZero 	   -6
#define  SWIG_OverflowError  	   -7
#define  SWIG_SyntaxError    	   -8
#define  SWIG_ValueError     	   -9
#define  SWIG_SystemError    	   -10
#define  SWIG_AttributeError 	   -11
#define  SWIG_MemoryError    	   -12
#define  SWIG_NullReferenceError   -13




#define SWIGVERSION 0x030012 
#define SWIG_VERSION SWIGVERSION


#define SWIG_as_voidptr(a) const_cast< void * >(static_cast< const void * >(a)) 
#define SWIG_as_voidptrptr(a) ((void)SWIG_as_voidptr(*a),reinterpret_cast< void** >(a)) 


#include <stdexcept>


#define SWIG_FILE_WITH_INIT

#include "spams.h"
#include "spams/prox/groups-graph.h"

#ifdef DEBUG
#include "spams-tst.h"
#endif


//extern "C" {
//#define NO_C_HEADERS (DEPRECATED, instead removed extern "C")
#include <R.h>
#include <Rinternals.h>
#include <Rdefines.h>
#include <R_ext/Rdynload.h>
#include <Rembedded.h>
#ifndef WIN32
#include <Rinterface.h>
#endif
#include <R_ext/RS.h>
#include <R_ext/Error.h>
//}
#include "spams.h"

/* ********************
* this is a hack to handle multiple values outrput
* return value is a vector initailized by swig in typemap out
* R_result_pos is the index in the vector : it must be set to 0 in typemap(out)
* typemap(argout) cannot be used without typemap(ou) in a function
*/

static int R_result_pos = 0;

SEXP appendOutput(SEXP value,SEXP result) {
    R_result_pos++;
    if(LENGTH(result) > R_result_pos)
        SET_VECTOR_ELT(result,R_result_pos,value);
    return result;
}
/*    end of hack */


SWIGINTERN char *
SWIG_strdup(const char *str)
{
  char *newstr = reinterpret_cast< char * >(malloc(strlen(str) + 1));
  return strcpy(newstr, str);
}


    const int check_sparse(SEXP arg) {
        SEXP ivec = Rf_getAttrib(arg,Rf_install("i"));
        SEXP xvec = Rf_getAttrib(arg,Rf_install("x"));
        SEXP dims = Rf_getAttrib(arg,Rf_install("Dim"));
        // return (Rf_isInteger(ivec) && (TYPEOF(xvec) == R_TYPE) && (LENGTH(dims) == 2) ? 1 : 0);
        return (Rf_isInteger(ivec) && (TYPEOF(xvec) == REALSXP) && (LENGTH(dims) == 2) ? 1 : 0);

    }
    const int check_matrix(SEXP arg) {
        SEXP dims = Rf_getAttrib(arg,Rf_install("dim"));
        return ((TYPEOF(arg) == REALSXP) && (LENGTH(dims) == 2) ? 1 : 0);
    }


#include <limits.h>
#if !defined(SWIG_NO_LLONG_MAX)
# if !defined(LLONG_MAX) && defined(__GNUC__) && defined (__LONG_LONG_MAX__)
#   define LLONG_MAX __LONG_LONG_MAX__
#   define LLONG_MIN (-LLONG_MAX - 1LL)
#   define ULLONG_MAX (LLONG_MAX * 2ULL + 1ULL)
# endif
#endif


SWIGINTERNINLINE  int
SWIG_AsVal_long (SEXP obj, long *val)
{
   if (val) *val = Rf_asInteger(obj);
   return SWIG_OK;
}


SWIGINTERN int
SWIG_AsVal_int (SEXP obj, int *val)
{
  long v;
  int res = SWIG_AsVal_long (obj, &v);
  if (SWIG_IsOK(res)) {
    if ((v < INT_MIN || v > INT_MAX)) {
      return SWIG_OverflowError;
    } else {
      if (val) *val = static_cast< int >(v);
    }
  }  
  return res;
}


SWIGINTERN int
SWIG_AsCharPtrAndSize(SEXP obj, char** cptr, size_t* psize, int *alloc)
{
  if (cptr && Rf_isString(obj)) {
    char *cstr = const_cast< char * >(CHAR(STRING_ELT(obj, 0)));
    int len = strlen(cstr);

    if (alloc) {
      if (*alloc == SWIG_NEWOBJ) {
        *cptr = reinterpret_cast< char* >(memcpy(new char[len + 1], cstr, sizeof(char)*(len + 1)));
        *alloc = SWIG_NEWOBJ;
      } else {
        *cptr = cstr;
      }
    } else {
      *cptr = reinterpret_cast< char * >(malloc(len + 1));
      *cptr = strcpy(*cptr, cstr);
    }
    if (psize) *psize = len + 1;
    return SWIG_OK;
  }
  return SWIG_TypeError;
}





#ifdef __cplusplus
extern "C" {
#endif

SWIGEXPORT SEXP
R_swig_m_ones ( SEXP nb)
{
  int *result = 0 ;
  int arg1 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  arg1 = static_cast< int >(INTEGER(nb)[0]);
  result = (int *)m_ones(arg1);
  r_ans = SWIG_R_NewPointerObj(SWIG_as_voidptr(result), SWIGTYPE_p_int, R_SWIG_EXTERNAL |  0 );
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_skip_space ( SEXP s)
{
  char *result = 0 ;
  char *arg1 = (char *) 0 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  arg1 = reinterpret_cast< char * >(SWIG_strdup(CHAR(STRING_ELT(s, 0))));
  result = (char *)skip_space(arg1);
  r_ans = result ? Rf_mkString(reinterpret_cast< char * >(result)) : R_NilValue;
  free(arg1);
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_parse_line ( SEXP s, SEXP vresult, SEXP s_swig_copy)
{
  int result;
  char *arg1 = (char *) 0 ;
  std::vector< std::string > *arg2 = 0 ;
  void *argp2 = 0 ;
  int res2 = 0 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  arg1 = reinterpret_cast< char * >(SWIG_strdup(CHAR(STRING_ELT(s, 0))));
  res2 = SWIG_R_ConvertPtr(vresult, &argp2, SWIGTYPE_p_std__vectorT_std__string_t,  0 );
  if (!SWIG_IsOK(res2)) {
    SWIG_exception_fail(SWIG_ArgError(res2), "in method '" "parse_line" "', argument " "2"" of type '" "std::vector< std::string > &""'"); 
  }
  if (!argp2) {
    SWIG_exception_fail(SWIG_ValueError, "invalid null reference " "in method '" "parse_line" "', argument " "2"" of type '" "std::vector< std::string > &""'"); 
  }
  arg2 = reinterpret_cast< std::vector< std::string > * >(argp2);
  result = (int)parse_line(arg1,*arg2);
  r_ans = Rf_ScalarInteger(result);
  free(arg1);
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_intlist ( SEXP s)
{
  std::vector< int > *result = 0 ;
  string arg1 ;
  void *argp1 ;
  int res1 = 0 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    res1 = SWIG_R_ConvertPtr(s, &argp1, SWIGTYPE_p_string,  0 );
    if (!SWIG_IsOK(res1)) {
      SWIG_exception_fail(SWIG_ArgError(res1), "in method '" "intlist" "', argument " "1"" of type '" "string""'"); 
    }  
    if (!argp1) {
      SWIG_exception_fail(SWIG_ValueError, "invalid null reference " "in method '" "intlist" "', argument " "1"" of type '" "string""'");
    } else {
      arg1 = *(reinterpret_cast< string * >(argp1));
    }
  }
  result = (std::vector< int > *)intlist(arg1);
  r_ans = SWIG_R_NewPointerObj(SWIG_as_voidptr(result), SWIGTYPE_p_std__vectorT_int_t, R_SWIG_EXTERNAL |  0 );
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_sort ( SEXP v, SEXP mode)
{
  Vector< double > *arg1 = (Vector< double > *) 0 ;
  bool arg2 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    SEXP rvec=v;
    if (TYPEOF(rvec) != REALSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double Vector as argument %d",1); return R_NilValue;
      };
    }
    
    arg1 = new Vector<double>((double*) REAL(rvec), LENGTH(rvec));
  }
  arg2 = LOGICAL(mode)[0] ? true : false;
  try {
    _sort< double >(arg1,arg2);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  r_ans = R_NilValue;
  {
    delete arg1;
  }
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_mult ( SEXP X, SEXP Y, SEXP XY, SEXP transX, SEXP transY, SEXP a, SEXP b)
{
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  Matrix< double > *arg3 = (Matrix< double > *) 0 ;
  bool arg4 ;
  bool arg5 ;
  double arg6 ;
  double arg7 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=Y;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=XY;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",3); return R_NilValue;
      };
    }
    arg3 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  arg4 = LOGICAL(transX)[0] ? true : false;
  arg5 = LOGICAL(transY)[0] ? true : false;
  arg6 = static_cast< double >(REAL(a)[0]);
  arg7 = static_cast< double >(REAL(b)[0]);
  try {
    _mult< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  r_ans = R_NilValue;
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  {
    delete arg3;
  }
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_AAt ( SEXP A, SEXP B)
{
  SpMatrix< double > *arg1 = (SpMatrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,227,map_sparse@*/
    SEXP spmat=A;
    SEXP ivec = Rf_getAttrib(spmat,Rf_install("i"));
    SEXP pvec = Rf_getAttrib(spmat,Rf_install("p"));
    SEXP xvec = Rf_getAttrib(spmat,Rf_install("x"));
    SEXP dims = Rf_getAttrib(spmat,Rf_install("Dim"));
    
    if (TYPEOF(xvec) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double sparse matrix as argument %d",1); return R_NilValue;
      };
    }
    int *pB = INTEGER(pvec);
    int *pE = pB + 1;
    int *idims = INTEGER(dims);
    arg1 = new SpMatrix<double> ((double *)REAL(xvec),INTEGER(ivec),pB,pE,idims[0],idims[1],LENGTH(xvec));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=B;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  try {
    _AAt< double >(arg1,arg2);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  r_ans = R_NilValue;
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_XAt ( SEXP A, SEXP X, SEXP XAt)
{
  SpMatrix< double > *arg1 = (SpMatrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  Matrix< double > *arg3 = (Matrix< double > *) 0 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,227,map_sparse@*/
    SEXP spmat=A;
    SEXP ivec = Rf_getAttrib(spmat,Rf_install("i"));
    SEXP pvec = Rf_getAttrib(spmat,Rf_install("p"));
    SEXP xvec = Rf_getAttrib(spmat,Rf_install("x"));
    SEXP dims = Rf_getAttrib(spmat,Rf_install("Dim"));
    
    if (TYPEOF(xvec) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double sparse matrix as argument %d",1); return R_NilValue;
      };
    }
    int *pB = INTEGER(pvec);
    int *pE = pB + 1;
    int *idims = INTEGER(dims);
    arg1 = new SpMatrix<double> ((double *)REAL(xvec),INTEGER(ivec),pB,pE,idims[0],idims[1],LENGTH(xvec));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=XAt;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",3); return R_NilValue;
      };
    }
    arg3 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  try {
    _XAt< double >(arg1,arg2,arg3);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  r_ans = R_NilValue;
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  {
    delete arg3;
  }
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_applyBayerPattern ( SEXP v, SEXP offset)
{
  Vector< double > *arg1 = (Vector< double > *) 0 ;
  int arg2 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    SEXP rvec=v;
    if (TYPEOF(rvec) != REALSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double Vector as argument %d",1); return R_NilValue;
      };
    }
    
    arg1 = new Vector<double>((double*) REAL(rvec), LENGTH(rvec));
  }
  arg2 = static_cast< int >(INTEGER(offset)[0]);
  try {
    _applyBayerPattern< double >(arg1,arg2);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  r_ans = R_NilValue;
  {
    delete arg1;
  }
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_conjugateGradient ( SEXP A, SEXP b, SEXP x, SEXP tol, SEXP itermax)
{
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Vector< double > *arg2 = (Vector< double > *) 0 ;
  Vector< double > *arg3 = (Vector< double > *) 0 ;
  double arg4 ;
  int arg5 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=A;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    SEXP rvec=b;
    if (TYPEOF(rvec) != REALSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double Vector as argument %d",2); return R_NilValue;
      };
    }
    
    arg2 = new Vector<double>((double*) REAL(rvec), LENGTH(rvec));
  }
  {
    SEXP rvec=x;
    if (TYPEOF(rvec) != REALSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double Vector as argument %d",3); return R_NilValue;
      };
    }
    
    arg3 = new Vector<double>((double*) REAL(rvec), LENGTH(rvec));
  }
  arg4 = static_cast< double >(REAL(tol)[0]);
  arg5 = static_cast< int >(INTEGER(itermax)[0]);
  try {
    _conjugateGradient< double >(arg1,arg2,arg3,arg4,arg5);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  r_ans = R_NilValue;
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  {
    delete arg3;
  }
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_invSym ( SEXP A)
{
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=A;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  _invSym< double >(arg1);
  r_ans = R_NilValue;
  {
    delete arg1;
  }
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_normalize ( SEXP A)
{
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=A;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  _normalize< double >(arg1);
  r_ans = R_NilValue;
  {
    delete arg1;
  }
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_sparseProject ( SEXP U, SEXP V, SEXP thrs, SEXP mode, SEXP lambda1, SEXP lambda2, SEXP lambda3, SEXP pos, SEXP numThreads)
{
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  double arg3 ;
  int arg4 ;
  double arg5 ;
  double arg6 ;
  double arg7 ;
  bool arg8 ;
  int arg9 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=U;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=V;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  arg3 = static_cast< double >(REAL(thrs)[0]);
  arg4 = static_cast< int >(INTEGER(mode)[0]);
  arg5 = static_cast< double >(REAL(lambda1)[0]);
  arg6 = static_cast< double >(REAL(lambda2)[0]);
  arg7 = static_cast< double >(REAL(lambda3)[0]);
  arg8 = LOGICAL(pos)[0] ? true : false;
  arg9 = static_cast< int >(INTEGER(numThreads)[0]);
  try {
    _sparseProject< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  r_ans = R_NilValue;
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  
  
  
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_lassoD ( SEXP X, SEXP D, SEXP return_reg_path, SEXP L, SEXP constraint, SEXP lambda2, SEXP mode, SEXP pos, SEXP ols, SEXP numThreads, SEXP max_length_path, SEXP verbose, SEXP cholevsky)
{
  SpMatrix< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  Matrix< double > **arg3 = (Matrix< double > **) 0 ;
  bool arg4 ;
  int arg5 ;
  double arg6 ;
  double arg7 ;
  constraint_type arg8 ;
  bool arg9 ;
  bool arg10 ;
  int arg11 ;
  int arg12 ;
  bool arg13 ;
  bool arg14 ;
  Matrix< double > *data_temp3 ;
  int val8 ;
  int ecode8 = 0 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  SEXP R_OutputValues;
  
  {
    arg3 = &data_temp3;
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=D;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  arg4 = LOGICAL(return_reg_path)[0] ? true : false;
  arg5 = static_cast< int >(INTEGER(L)[0]);
  arg6 = static_cast< double >(REAL(constraint)[0]);
  arg7 = static_cast< double >(REAL(lambda2)[0]);
  ecode8 = SWIG_AsVal_int(mode, &val8);
  if (!SWIG_IsOK(ecode8)) {
    SWIG_exception_fail(SWIG_ArgError(ecode8), "in method '" "lassoD" "', argument " "8"" of type '" "constraint_type""'");
  } 
  arg8 = static_cast< constraint_type >(val8);
  arg9 = LOGICAL(pos)[0] ? true : false;
  arg10 = LOGICAL(ols)[0] ? true : false;
  arg11 = static_cast< int >(INTEGER(numThreads)[0]);
  arg12 = static_cast< int >(INTEGER(max_length_path)[0]);
  arg13 = LOGICAL(verbose)[0] ? true : false;
  arg14 = LOGICAL(cholevsky)[0] ? true : false;
  try {
    result = (SpMatrix< double > *)_lassoD< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9,arg10,arg11,arg12,arg13,arg14);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t m = result->m();
    R_len_t n = result->n();
    R_len_t nzmax = result->nzmax();
    SEXP indptr, indices, vdata, dims, output;
    PROTECT(indptr = Rf_allocVector(INTSXP,n + 1));
    PROTECT(indices = Rf_allocVector(INTSXP,nzmax));
    PROTECT(vdata = Rf_allocVector(REALSXP,nzmax));
    PROTECT(dims = Rf_allocVector(VECSXP,2));
    SET_VECTOR_ELT(dims,0,Rf_ScalarInteger(m));
    SET_VECTOR_ELT(dims,1,Rf_ScalarInteger(n));
    
    double *xdata = result->v();
    memcpy(REAL(vdata),xdata,nzmax * sizeof(double));
    int *pB = result->pB();
    int *r = result->r();
    memcpy(INTEGER(indices),r,nzmax * sizeof(int));
    memcpy(INTEGER(indptr),pB,(n + 1) * sizeof(int));
    
    PROTECT(output = Rf_allocVector(VECSXP,4));
    SET_VECTOR_ELT(output,0,indptr);
    SET_VECTOR_ELT(output,1,indices);
    SET_VECTOR_ELT(output,2,vdata);
    SET_VECTOR_ELT(output,3,dims);
    delete result;
    r_ans = output;
    UNPROTECT(5);
  }
  Rf_protect(r_ans);
  Rf_protect(R_OutputValues = Rf_allocVector(VECSXP,2));
  r_nprotect += 2;
  SET_VECTOR_ELT(R_OutputValues, 0, r_ans);
  r_ans = R_OutputValues;
  {
    //# test argout
    if(data_temp3 != NULL) {
      R_len_t m = data_temp3->m();
      R_len_t n = data_temp3->n();
      double *data = data_temp3->rawX();
      SEXP rmat;
      PROTECT(rmat = Rf_allocMatrix(REALSXP,m,n));
      if (!rmat) {
        Rf_error("Cannot alloc R matrix for arg %d",3); return R_NilValue;
      };
      double *rdata = (double *)REAL(rmat);
      memcpy(rdata,data,m * n * sizeof(double));
      delete data_temp3;
      r_ans = appendOutput(rmat, R_OutputValues);;
      UNPROTECT(1);
    }
  }
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  
  
  
  
  
  
  
  
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_lassoQq ( SEXP X, SEXP Q, SEXP q, SEXP return_reg_path, SEXP L, SEXP constraint, SEXP lambda2, SEXP mode, SEXP pos, SEXP ols, SEXP numThreads, SEXP max_length_path, SEXP verbose, SEXP cholevsky)
{
  SpMatrix< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  Matrix< double > *arg3 = (Matrix< double > *) 0 ;
  Matrix< double > **arg4 = (Matrix< double > **) 0 ;
  bool arg5 ;
  int arg6 ;
  double arg7 ;
  double arg8 ;
  constraint_type arg9 ;
  bool arg10 ;
  bool arg11 ;
  int arg12 ;
  int arg13 ;
  bool arg14 ;
  bool arg15 ;
  Matrix< double > *data_temp4 ;
  int val9 ;
  int ecode9 = 0 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  SEXP R_OutputValues;
  
  {
    arg4 = &data_temp4;
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=Q;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=q;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",3); return R_NilValue;
      };
    }
    arg3 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  arg5 = LOGICAL(return_reg_path)[0] ? true : false;
  arg6 = static_cast< int >(INTEGER(L)[0]);
  arg7 = static_cast< double >(REAL(constraint)[0]);
  arg8 = static_cast< double >(REAL(lambda2)[0]);
  ecode9 = SWIG_AsVal_int(mode, &val9);
  if (!SWIG_IsOK(ecode9)) {
    SWIG_exception_fail(SWIG_ArgError(ecode9), "in method '" "lassoQq" "', argument " "9"" of type '" "constraint_type""'");
  } 
  arg9 = static_cast< constraint_type >(val9);
  arg10 = LOGICAL(pos)[0] ? true : false;
  arg11 = LOGICAL(ols)[0] ? true : false;
  arg12 = static_cast< int >(INTEGER(numThreads)[0]);
  arg13 = static_cast< int >(INTEGER(max_length_path)[0]);
  arg14 = LOGICAL(verbose)[0] ? true : false;
  arg15 = LOGICAL(cholevsky)[0] ? true : false;
  try {
    result = (SpMatrix< double > *)_lassoQq< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9,arg10,arg11,arg12,arg13,arg14,arg15);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t m = result->m();
    R_len_t n = result->n();
    R_len_t nzmax = result->nzmax();
    SEXP indptr, indices, vdata, dims, output;
    PROTECT(indptr = Rf_allocVector(INTSXP,n + 1));
    PROTECT(indices = Rf_allocVector(INTSXP,nzmax));
    PROTECT(vdata = Rf_allocVector(REALSXP,nzmax));
    PROTECT(dims = Rf_allocVector(VECSXP,2));
    SET_VECTOR_ELT(dims,0,Rf_ScalarInteger(m));
    SET_VECTOR_ELT(dims,1,Rf_ScalarInteger(n));
    
    double *xdata = result->v();
    memcpy(REAL(vdata),xdata,nzmax * sizeof(double));
    int *pB = result->pB();
    int *r = result->r();
    memcpy(INTEGER(indices),r,nzmax * sizeof(int));
    memcpy(INTEGER(indptr),pB,(n + 1) * sizeof(int));
    
    PROTECT(output = Rf_allocVector(VECSXP,4));
    SET_VECTOR_ELT(output,0,indptr);
    SET_VECTOR_ELT(output,1,indices);
    SET_VECTOR_ELT(output,2,vdata);
    SET_VECTOR_ELT(output,3,dims);
    delete result;
    r_ans = output;
    UNPROTECT(5);
  }
  Rf_protect(r_ans);
  Rf_protect(R_OutputValues = Rf_allocVector(VECSXP,2));
  r_nprotect += 2;
  SET_VECTOR_ELT(R_OutputValues, 0, r_ans);
  r_ans = R_OutputValues;
  {
    //# test argout
    if(data_temp4 != NULL) {
      R_len_t m = data_temp4->m();
      R_len_t n = data_temp4->n();
      double *data = data_temp4->rawX();
      SEXP rmat;
      PROTECT(rmat = Rf_allocMatrix(REALSXP,m,n));
      if (!rmat) {
        Rf_error("Cannot alloc R matrix for arg %d",4); return R_NilValue;
      };
      double *rdata = (double *)REAL(rmat);
      memcpy(rdata,data,m * n * sizeof(double));
      delete data_temp4;
      r_ans = appendOutput(rmat, R_OutputValues);;
      UNPROTECT(1);
    }
  }
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  {
    delete arg3;
  }
  
  
  
  
  
  
  
  
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_lassoMask ( SEXP X, SEXP D, SEXP B, SEXP L, SEXP constraint, SEXP lambda2, SEXP mode, SEXP pos, SEXP numThreads, SEXP verbose)
{
  SpMatrix< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  Matrix< bool > *arg3 = (Matrix< bool > *) 0 ;
  int arg4 ;
  double arg5 ;
  double arg6 ;
  constraint_type arg7 ;
  bool arg8 ;
  int arg9 ;
  bool arg10 ;
  int val7 ;
  int ecode7 = 0 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=D;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    SEXP rmat=B;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != LGLSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected bool dense matrix as argument %d",3); return R_NilValue;
      };
    }
    arg3 = new Matrix<bool> (Rf_nrows(rmat),Rf_ncols(rmat));
    int *pi = (int *)LOGICAL(rmat);
    bool *po = arg3->rawX();
    for(int i =0;i < Rf_nrows(rmat) * Rf_ncols(rmat);i++)
    *po++ = (bool) *pi++;
    
  }
  arg4 = static_cast< int >(INTEGER(L)[0]);
  arg5 = static_cast< double >(REAL(constraint)[0]);
  arg6 = static_cast< double >(REAL(lambda2)[0]);
  ecode7 = SWIG_AsVal_int(mode, &val7);
  if (!SWIG_IsOK(ecode7)) {
    SWIG_exception_fail(SWIG_ArgError(ecode7), "in method '" "lassoMask" "', argument " "7"" of type '" "constraint_type""'");
  } 
  arg7 = static_cast< constraint_type >(val7);
  arg8 = LOGICAL(pos)[0] ? true : false;
  arg9 = static_cast< int >(INTEGER(numThreads)[0]);
  arg10 = LOGICAL(verbose)[0] ? true : false;
  try {
    result = (SpMatrix< double > *)_lassoMask< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9,arg10);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t m = result->m();
    R_len_t n = result->n();
    R_len_t nzmax = result->nzmax();
    SEXP indptr, indices, vdata, dims, output;
    PROTECT(indptr = Rf_allocVector(INTSXP,n + 1));
    PROTECT(indices = Rf_allocVector(INTSXP,nzmax));
    PROTECT(vdata = Rf_allocVector(REALSXP,nzmax));
    PROTECT(dims = Rf_allocVector(VECSXP,2));
    SET_VECTOR_ELT(dims,0,Rf_ScalarInteger(m));
    SET_VECTOR_ELT(dims,1,Rf_ScalarInteger(n));
    
    double *xdata = result->v();
    memcpy(REAL(vdata),xdata,nzmax * sizeof(double));
    int *pB = result->pB();
    int *r = result->r();
    memcpy(INTEGER(indices),r,nzmax * sizeof(int));
    memcpy(INTEGER(indptr),pB,(n + 1) * sizeof(int));
    
    PROTECT(output = Rf_allocVector(VECSXP,4));
    SET_VECTOR_ELT(output,0,indptr);
    SET_VECTOR_ELT(output,1,indices);
    SET_VECTOR_ELT(output,2,vdata);
    SET_VECTOR_ELT(output,3,dims);
    delete result;
    r_ans = output;
    UNPROTECT(5);
  }
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  {
    delete arg3;
  }
  
  
  
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_lassoWeighted ( SEXP X, SEXP D, SEXP W, SEXP L, SEXP constraint, SEXP mode, SEXP pos, SEXP numThreads, SEXP verbose)
{
  SpMatrix< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  Matrix< double > *arg3 = (Matrix< double > *) 0 ;
  int arg4 ;
  double arg5 ;
  constraint_type arg6 ;
  bool arg7 ;
  int arg8 ;
  bool arg9 ;
  int val6 ;
  int ecode6 = 0 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=D;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=W;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",3); return R_NilValue;
      };
    }
    arg3 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  arg4 = static_cast< int >(INTEGER(L)[0]);
  arg5 = static_cast< double >(REAL(constraint)[0]);
  ecode6 = SWIG_AsVal_int(mode, &val6);
  if (!SWIG_IsOK(ecode6)) {
    SWIG_exception_fail(SWIG_ArgError(ecode6), "in method '" "lassoWeighted" "', argument " "6"" of type '" "constraint_type""'");
  } 
  arg6 = static_cast< constraint_type >(val6);
  arg7 = LOGICAL(pos)[0] ? true : false;
  arg8 = static_cast< int >(INTEGER(numThreads)[0]);
  arg9 = LOGICAL(verbose)[0] ? true : false;
  try {
    result = (SpMatrix< double > *)_lassoWeighted< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t m = result->m();
    R_len_t n = result->n();
    R_len_t nzmax = result->nzmax();
    SEXP indptr, indices, vdata, dims, output;
    PROTECT(indptr = Rf_allocVector(INTSXP,n + 1));
    PROTECT(indices = Rf_allocVector(INTSXP,nzmax));
    PROTECT(vdata = Rf_allocVector(REALSXP,nzmax));
    PROTECT(dims = Rf_allocVector(VECSXP,2));
    SET_VECTOR_ELT(dims,0,Rf_ScalarInteger(m));
    SET_VECTOR_ELT(dims,1,Rf_ScalarInteger(n));
    
    double *xdata = result->v();
    memcpy(REAL(vdata),xdata,nzmax * sizeof(double));
    int *pB = result->pB();
    int *r = result->r();
    memcpy(INTEGER(indices),r,nzmax * sizeof(int));
    memcpy(INTEGER(indptr),pB,(n + 1) * sizeof(int));
    
    PROTECT(output = Rf_allocVector(VECSXP,4));
    SET_VECTOR_ELT(output,0,indptr);
    SET_VECTOR_ELT(output,1,indices);
    SET_VECTOR_ELT(output,2,vdata);
    SET_VECTOR_ELT(output,3,dims);
    delete result;
    r_ans = output;
    UNPROTECT(5);
  }
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  {
    delete arg3;
  }
  
  
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_omp ( SEXP X, SEXP D, SEXP return_reg_path, SEXP given_L, SEXP L, SEXP given_eps, SEXP eps, SEXP given_Lambda, SEXP Lambda, SEXP numThreads)
{
  SpMatrix< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  Matrix< double > **arg3 = (Matrix< double > **) 0 ;
  bool arg4 ;
  bool arg5 ;
  Vector< int > *arg6 = (Vector< int > *) 0 ;
  bool arg7 ;
  Vector< double > *arg8 = (Vector< double > *) 0 ;
  bool arg9 ;
  Vector< double > *arg10 = (Vector< double > *) 0 ;
  int arg11 ;
  Matrix< double > *data_temp3 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  SEXP R_OutputValues;
  
  {
    arg3 = &data_temp3;
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=D;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  arg4 = LOGICAL(return_reg_path)[0] ? true : false;
  arg5 = LOGICAL(given_L)[0] ? true : false;
  {
    SEXP rvec=L;
    if (TYPEOF(rvec) != INTSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected int Vector as argument %d",6); return R_NilValue;
      };
    }
    
    arg6 = new Vector<int>((int*) INTEGER(rvec), LENGTH(rvec));
  }
  arg7 = LOGICAL(given_eps)[0] ? true : false;
  {
    SEXP rvec=eps;
    if (TYPEOF(rvec) != REALSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double Vector as argument %d",8); return R_NilValue;
      };
    }
    
    arg8 = new Vector<double>((double*) REAL(rvec), LENGTH(rvec));
  }
  arg9 = LOGICAL(given_Lambda)[0] ? true : false;
  {
    SEXP rvec=Lambda;
    if (TYPEOF(rvec) != REALSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double Vector as argument %d",10); return R_NilValue;
      };
    }
    
    arg10 = new Vector<double>((double*) REAL(rvec), LENGTH(rvec));
  }
  arg11 = static_cast< int >(INTEGER(numThreads)[0]);
  try {
    result = (SpMatrix< double > *)_omp< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9,arg10,arg11);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t m = result->m();
    R_len_t n = result->n();
    R_len_t nzmax = result->nzmax();
    SEXP indptr, indices, vdata, dims, output;
    PROTECT(indptr = Rf_allocVector(INTSXP,n + 1));
    PROTECT(indices = Rf_allocVector(INTSXP,nzmax));
    PROTECT(vdata = Rf_allocVector(REALSXP,nzmax));
    PROTECT(dims = Rf_allocVector(VECSXP,2));
    SET_VECTOR_ELT(dims,0,Rf_ScalarInteger(m));
    SET_VECTOR_ELT(dims,1,Rf_ScalarInteger(n));
    
    double *xdata = result->v();
    memcpy(REAL(vdata),xdata,nzmax * sizeof(double));
    int *pB = result->pB();
    int *r = result->r();
    memcpy(INTEGER(indices),r,nzmax * sizeof(int));
    memcpy(INTEGER(indptr),pB,(n + 1) * sizeof(int));
    
    PROTECT(output = Rf_allocVector(VECSXP,4));
    SET_VECTOR_ELT(output,0,indptr);
    SET_VECTOR_ELT(output,1,indices);
    SET_VECTOR_ELT(output,2,vdata);
    SET_VECTOR_ELT(output,3,dims);
    delete result;
    r_ans = output;
    UNPROTECT(5);
  }
  Rf_protect(r_ans);
  Rf_protect(R_OutputValues = Rf_allocVector(VECSXP,2));
  r_nprotect += 2;
  SET_VECTOR_ELT(R_OutputValues, 0, r_ans);
  r_ans = R_OutputValues;
  {
    //# test argout
    if(data_temp3 != NULL) {
      R_len_t m = data_temp3->m();
      R_len_t n = data_temp3->n();
      double *data = data_temp3->rawX();
      SEXP rmat;
      PROTECT(rmat = Rf_allocMatrix(REALSXP,m,n));
      if (!rmat) {
        Rf_error("Cannot alloc R matrix for arg %d",3); return R_NilValue;
      };
      double *rdata = (double *)REAL(rmat);
      memcpy(rdata,data,m * n * sizeof(double));
      delete data_temp3;
      r_ans = appendOutput(rmat, R_OutputValues);;
      UNPROTECT(1);
    }
  }
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  
  
  
  {
    delete arg6;
  }
  
  {
    delete arg8;
  }
  
  {
    delete arg10;
  }
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_ompMask ( SEXP X, SEXP D, SEXP B, SEXP return_reg_path, SEXP given_L, SEXP L, SEXP given_eps, SEXP eps, SEXP given_Lambda, SEXP Lambda, SEXP numThreads)
{
  SpMatrix< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  Matrix< bool > *arg3 = (Matrix< bool > *) 0 ;
  Matrix< double > **arg4 = (Matrix< double > **) 0 ;
  bool arg5 ;
  bool arg6 ;
  Vector< int > *arg7 = (Vector< int > *) 0 ;
  bool arg8 ;
  Vector< double > *arg9 = (Vector< double > *) 0 ;
  bool arg10 ;
  Vector< double > *arg11 = (Vector< double > *) 0 ;
  int arg12 ;
  Matrix< double > *data_temp4 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  SEXP R_OutputValues;
  
  {
    arg4 = &data_temp4;
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=D;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    SEXP rmat=B;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != LGLSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected bool dense matrix as argument %d",3); return R_NilValue;
      };
    }
    arg3 = new Matrix<bool> (Rf_nrows(rmat),Rf_ncols(rmat));
    int *pi = (int *)LOGICAL(rmat);
    bool *po = arg3->rawX();
    for(int i =0;i < Rf_nrows(rmat) * Rf_ncols(rmat);i++)
    *po++ = (bool) *pi++;
    
  }
  arg5 = LOGICAL(return_reg_path)[0] ? true : false;
  arg6 = LOGICAL(given_L)[0] ? true : false;
  {
    SEXP rvec=L;
    if (TYPEOF(rvec) != INTSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected int Vector as argument %d",7); return R_NilValue;
      };
    }
    
    arg7 = new Vector<int>((int*) INTEGER(rvec), LENGTH(rvec));
  }
  arg8 = LOGICAL(given_eps)[0] ? true : false;
  {
    SEXP rvec=eps;
    if (TYPEOF(rvec) != REALSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double Vector as argument %d",9); return R_NilValue;
      };
    }
    
    arg9 = new Vector<double>((double*) REAL(rvec), LENGTH(rvec));
  }
  arg10 = LOGICAL(given_Lambda)[0] ? true : false;
  {
    SEXP rvec=Lambda;
    if (TYPEOF(rvec) != REALSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double Vector as argument %d",11); return R_NilValue;
      };
    }
    
    arg11 = new Vector<double>((double*) REAL(rvec), LENGTH(rvec));
  }
  arg12 = static_cast< int >(INTEGER(numThreads)[0]);
  try {
    result = (SpMatrix< double > *)_ompMask< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9,arg10,arg11,arg12);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t m = result->m();
    R_len_t n = result->n();
    R_len_t nzmax = result->nzmax();
    SEXP indptr, indices, vdata, dims, output;
    PROTECT(indptr = Rf_allocVector(INTSXP,n + 1));
    PROTECT(indices = Rf_allocVector(INTSXP,nzmax));
    PROTECT(vdata = Rf_allocVector(REALSXP,nzmax));
    PROTECT(dims = Rf_allocVector(VECSXP,2));
    SET_VECTOR_ELT(dims,0,Rf_ScalarInteger(m));
    SET_VECTOR_ELT(dims,1,Rf_ScalarInteger(n));
    
    double *xdata = result->v();
    memcpy(REAL(vdata),xdata,nzmax * sizeof(double));
    int *pB = result->pB();
    int *r = result->r();
    memcpy(INTEGER(indices),r,nzmax * sizeof(int));
    memcpy(INTEGER(indptr),pB,(n + 1) * sizeof(int));
    
    PROTECT(output = Rf_allocVector(VECSXP,4));
    SET_VECTOR_ELT(output,0,indptr);
    SET_VECTOR_ELT(output,1,indices);
    SET_VECTOR_ELT(output,2,vdata);
    SET_VECTOR_ELT(output,3,dims);
    delete result;
    r_ans = output;
    UNPROTECT(5);
  }
  Rf_protect(r_ans);
  Rf_protect(R_OutputValues = Rf_allocVector(VECSXP,2));
  r_nprotect += 2;
  SET_VECTOR_ELT(R_OutputValues, 0, r_ans);
  r_ans = R_OutputValues;
  {
    //# test argout
    if(data_temp4 != NULL) {
      R_len_t m = data_temp4->m();
      R_len_t n = data_temp4->n();
      double *data = data_temp4->rawX();
      SEXP rmat;
      PROTECT(rmat = Rf_allocMatrix(REALSXP,m,n));
      if (!rmat) {
        Rf_error("Cannot alloc R matrix for arg %d",4); return R_NilValue;
      };
      double *rdata = (double *)REAL(rmat);
      memcpy(rdata,data,m * n * sizeof(double));
      delete data_temp4;
      r_ans = appendOutput(rmat, R_OutputValues);;
      UNPROTECT(1);
    }
  }
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  {
    delete arg3;
  }
  
  
  
  {
    delete arg7;
  }
  
  {
    delete arg9;
  }
  
  {
    delete arg11;
  }
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_somp ( SEXP X, SEXP D, SEXP groups, SEXP LL, SEXP eps, SEXP numThreads)
{
  SpMatrix< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  Vector< int > *arg3 = (Vector< int > *) 0 ;
  int arg4 ;
  double arg5 ;
  int arg6 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=D;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    SEXP rvec=groups;
    if (TYPEOF(rvec) != INTSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected int Vector as argument %d",3); return R_NilValue;
      };
    }
    
    arg3 = new Vector<int>((int*) INTEGER(rvec), LENGTH(rvec));
  }
  arg4 = static_cast< int >(INTEGER(LL)[0]);
  arg5 = static_cast< double >(REAL(eps)[0]);
  arg6 = static_cast< int >(INTEGER(numThreads)[0]);
  try {
    result = (SpMatrix< double > *)_somp< double >(arg1,arg2,arg3,arg4,arg5,arg6);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t m = result->m();
    R_len_t n = result->n();
    R_len_t nzmax = result->nzmax();
    SEXP indptr, indices, vdata, dims, output;
    PROTECT(indptr = Rf_allocVector(INTSXP,n + 1));
    PROTECT(indices = Rf_allocVector(INTSXP,nzmax));
    PROTECT(vdata = Rf_allocVector(REALSXP,nzmax));
    PROTECT(dims = Rf_allocVector(VECSXP,2));
    SET_VECTOR_ELT(dims,0,Rf_ScalarInteger(m));
    SET_VECTOR_ELT(dims,1,Rf_ScalarInteger(n));
    
    double *xdata = result->v();
    memcpy(REAL(vdata),xdata,nzmax * sizeof(double));
    int *pB = result->pB();
    int *r = result->r();
    memcpy(INTEGER(indices),r,nzmax * sizeof(int));
    memcpy(INTEGER(indptr),pB,(n + 1) * sizeof(int));
    
    PROTECT(output = Rf_allocVector(VECSXP,4));
    SET_VECTOR_ELT(output,0,indptr);
    SET_VECTOR_ELT(output,1,indices);
    SET_VECTOR_ELT(output,2,vdata);
    SET_VECTOR_ELT(output,3,dims);
    delete result;
    r_ans = output;
    UNPROTECT(5);
  }
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  {
    delete arg3;
  }
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_cd ( SEXP X, SEXP D, SEXP alpha, SEXP lambda1, SEXP mode, SEXP itermax, SEXP tol, SEXP numThreads)
{
  SpMatrix< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  SpMatrix< double > *arg3 = (SpMatrix< double > *) 0 ;
  double arg4 ;
  constraint_type arg5 ;
  int arg6 ;
  double arg7 ;
  int arg8 ;
  int val5 ;
  int ecode5 = 0 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=D;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,227,map_sparse@*/
    SEXP spmat=alpha;
    SEXP ivec = Rf_getAttrib(spmat,Rf_install("i"));
    SEXP pvec = Rf_getAttrib(spmat,Rf_install("p"));
    SEXP xvec = Rf_getAttrib(spmat,Rf_install("x"));
    SEXP dims = Rf_getAttrib(spmat,Rf_install("Dim"));
    
    if (TYPEOF(xvec) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double sparse matrix as argument %d",3); return R_NilValue;
      };
    }
    int *pB = INTEGER(pvec);
    int *pE = pB + 1;
    int *idims = INTEGER(dims);
    arg3 = new SpMatrix<double> ((double *)REAL(xvec),INTEGER(ivec),pB,pE,idims[0],idims[1],LENGTH(xvec));
    
    /*@SWIG@*/
  }
  arg4 = static_cast< double >(REAL(lambda1)[0]);
  ecode5 = SWIG_AsVal_int(mode, &val5);
  if (!SWIG_IsOK(ecode5)) {
    SWIG_exception_fail(SWIG_ArgError(ecode5), "in method '" "cd" "', argument " "5"" of type '" "constraint_type""'");
  } 
  arg5 = static_cast< constraint_type >(val5);
  arg6 = static_cast< int >(INTEGER(itermax)[0]);
  arg7 = static_cast< double >(REAL(tol)[0]);
  arg8 = static_cast< int >(INTEGER(numThreads)[0]);
  try {
    result = (SpMatrix< double > *)_cd< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t m = result->m();
    R_len_t n = result->n();
    R_len_t nzmax = result->nzmax();
    SEXP indptr, indices, vdata, dims, output;
    PROTECT(indptr = Rf_allocVector(INTSXP,n + 1));
    PROTECT(indices = Rf_allocVector(INTSXP,nzmax));
    PROTECT(vdata = Rf_allocVector(REALSXP,nzmax));
    PROTECT(dims = Rf_allocVector(VECSXP,2));
    SET_VECTOR_ELT(dims,0,Rf_ScalarInteger(m));
    SET_VECTOR_ELT(dims,1,Rf_ScalarInteger(n));
    
    double *xdata = result->v();
    memcpy(REAL(vdata),xdata,nzmax * sizeof(double));
    int *pB = result->pB();
    int *r = result->r();
    memcpy(INTEGER(indices),r,nzmax * sizeof(int));
    memcpy(INTEGER(indptr),pB,(n + 1) * sizeof(int));
    
    PROTECT(output = Rf_allocVector(VECSXP,4));
    SET_VECTOR_ELT(output,0,indptr);
    SET_VECTOR_ELT(output,1,indices);
    SET_VECTOR_ELT(output,2,vdata);
    SET_VECTOR_ELT(output,3,dims);
    delete result;
    r_ans = output;
    UNPROTECT(5);
  }
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  {
    delete arg3;
  }
  
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_l1L2BCD ( SEXP X, SEXP D, SEXP alpha0, SEXP groups, SEXP lambda1, SEXP mode, SEXP itermax, SEXP tol, SEXP numThreads)
{
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  Matrix< double > *arg3 = (Matrix< double > *) 0 ;
  Vector< int > *arg4 = (Vector< int > *) 0 ;
  double arg5 ;
  constraint_type arg6 ;
  int arg7 ;
  double arg8 ;
  int arg9 ;
  int val6 ;
  int ecode6 = 0 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=D;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=alpha0;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",3); return R_NilValue;
      };
    }
    arg3 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    SEXP rvec=groups;
    if (TYPEOF(rvec) != INTSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected int Vector as argument %d",4); return R_NilValue;
      };
    }
    
    arg4 = new Vector<int>((int*) INTEGER(rvec), LENGTH(rvec));
  }
  arg5 = static_cast< double >(REAL(lambda1)[0]);
  ecode6 = SWIG_AsVal_int(mode, &val6);
  if (!SWIG_IsOK(ecode6)) {
    SWIG_exception_fail(SWIG_ArgError(ecode6), "in method '" "l1L2BCD" "', argument " "6"" of type '" "constraint_type""'");
  } 
  arg6 = static_cast< constraint_type >(val6);
  arg7 = static_cast< int >(INTEGER(itermax)[0]);
  arg8 = static_cast< double >(REAL(tol)[0]);
  arg9 = static_cast< int >(INTEGER(numThreads)[0]);
  try {
    _l1L2BCD< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  r_ans = R_NilValue;
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  {
    delete arg3;
  }
  {
    delete arg4;
  }
  
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_alltrainDL ( SEXP X, SEXP in_memory, SEXP return_model, SEXP m_A, SEXP m_B, SEXP m_iter, SEXP D1, SEXP eta_g, SEXP groups, SEXP groups_var, SEXP own_variables, SEXP N_own_variables, SEXP num_threads, SEXP tol, SEXP fixed_step, SEXP ista, SEXP batch_size, SEXP K, SEXP lambda1, SEXP lambda2, SEXP lambda3, SEXP iter, SEXP t0, SEXP mode, SEXP name_regul, SEXP posAlpha, SEXP posD, SEXP expand, SEXP modeD, SEXP whiten, SEXP clean, SEXP verbose, SEXP gamma1, SEXP gamma2, SEXP rho, SEXP iter_updateD, SEXP stochastic, SEXP modeParam, SEXP batch, SEXP log, SEXP logName)
{
  Matrix< double > *result = 0 ;
  Data< double > *arg1 = (Data< double > *) 0 ;
  bool arg2 ;
  Matrix< double > **arg3 = (Matrix< double > **) 0 ;
  Matrix< double > **arg4 = (Matrix< double > **) 0 ;
  Vector< int > **arg5 = (Vector< int > **) 0 ;
  bool arg6 ;
  Matrix< double > *arg7 = (Matrix< double > *) 0 ;
  Matrix< double > *arg8 = (Matrix< double > *) 0 ;
  int arg9 ;
  Matrix< double > *arg10 = (Matrix< double > *) 0 ;
  Vector< double > *arg11 = (Vector< double > *) 0 ;
  SpMatrix< bool > *arg12 = (SpMatrix< bool > *) 0 ;
  SpMatrix< bool > *arg13 = (SpMatrix< bool > *) 0 ;
  Vector< int > *arg14 = (Vector< int > *) 0 ;
  Vector< int > *arg15 = (Vector< int > *) 0 ;
  int arg16 ;
  double arg17 ;
  bool arg18 ;
  bool arg19 ;
  int arg20 ;
  int arg21 ;
  double arg22 ;
  double arg23 ;
  double arg24 ;
  int arg25 ;
  double arg26 ;
  constraint_type arg27 ;
  char *arg28 = (char *) 0 ;
  bool arg29 ;
  bool arg30 ;
  bool arg31 ;
  constraint_type_D arg32 ;
  bool arg33 ;
  bool arg34 ;
  bool arg35 ;
  double arg36 ;
  double arg37 ;
  double arg38 ;
  int arg39 ;
  bool arg40 ;
  int arg41 ;
  bool arg42 ;
  bool arg43 ;
  char *arg44 = (char *) 0 ;
  Matrix< double > *data_temp3 ;
  Matrix< double > *data_temp4 ;
  Vector< int > *data_temp5 ;
  int val27 ;
  int ecode27 = 0 ;
  int val32 ;
  int ecode32 = 0 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  SEXP R_OutputValues;
  
  {
    arg3 = &data_temp3;
  }
  {
    arg4 = &data_temp4;
  }
  {
    arg5 = &data_temp5;
  }
  {
    SEXP pvec = Rf_getAttrib(X,Rf_install("p"));
    if(pvec ==  R_NilValue) {
      /*@SWIG:R_typemaps.i,113,map_matrix@*/
      SEXP rmat=X;
      SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
      if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
      {
        /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
        {
          Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
        };
      }
      arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
      
      /*@SWIG@*/
    } else {
      /*@SWIG:R_typemaps.i,227,map_sparse@*/
      SEXP spmat=X;
      SEXP ivec = Rf_getAttrib(spmat,Rf_install("i"));
      SEXP pvec = Rf_getAttrib(spmat,Rf_install("p"));
      SEXP xvec = Rf_getAttrib(spmat,Rf_install("x"));
      SEXP dims = Rf_getAttrib(spmat,Rf_install("Dim"));
      
      if (TYPEOF(xvec) != REALSXP || LENGTH(dims) != 2)
      {
        /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
        {
          Rf_error("Expected double sparse matrix as argument %d",1); return R_NilValue;
        };
      }
      int *pB = INTEGER(pvec);
      int *pE = pB + 1;
      int *idims = INTEGER(dims);
      arg1 = new SpMatrix<double> ((double *)REAL(xvec),INTEGER(ivec),pB,pE,idims[0],idims[1],LENGTH(xvec));
      
      /*@SWIG@*/
    }
  }
  arg2 = LOGICAL(in_memory)[0] ? true : false;
  arg6 = LOGICAL(return_model)[0] ? true : false;
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=m_A;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",7); return R_NilValue;
      };
    }
    arg7 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=m_B;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",8); return R_NilValue;
      };
    }
    arg8 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  arg9 = static_cast< int >(INTEGER(m_iter)[0]);
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=D1;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",10); return R_NilValue;
      };
    }
    arg10 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    SEXP rvec=eta_g;
    if (TYPEOF(rvec) != REALSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double Vector as argument %d",11); return R_NilValue;
      };
    }
    
    arg11 = new Vector<double>((double*) REAL(rvec), LENGTH(rvec));
  }
  {
    /*@SWIG:R_typemaps.i,227,map_sparse@*/
    SEXP spmat=groups;
    SEXP ivec = Rf_getAttrib(spmat,Rf_install("i"));
    SEXP pvec = Rf_getAttrib(spmat,Rf_install("p"));
    SEXP xvec = Rf_getAttrib(spmat,Rf_install("x"));
    SEXP dims = Rf_getAttrib(spmat,Rf_install("Dim"));
    
    if (TYPEOF(xvec) != LGLSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected bool sparse matrix as argument %d",12); return R_NilValue;
      };
    }
    int *pB = INTEGER(pvec);
    int *pE = pB + 1;
    int *idims = INTEGER(dims);
    arg12 = new SpMatrix<bool> ((bool *)LOGICAL(xvec),INTEGER(ivec),pB,pE,idims[0],idims[1],LENGTH(xvec));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,227,map_sparse@*/
    SEXP spmat=groups_var;
    SEXP ivec = Rf_getAttrib(spmat,Rf_install("i"));
    SEXP pvec = Rf_getAttrib(spmat,Rf_install("p"));
    SEXP xvec = Rf_getAttrib(spmat,Rf_install("x"));
    SEXP dims = Rf_getAttrib(spmat,Rf_install("Dim"));
    
    if (TYPEOF(xvec) != LGLSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected bool sparse matrix as argument %d",13); return R_NilValue;
      };
    }
    int *pB = INTEGER(pvec);
    int *pE = pB + 1;
    int *idims = INTEGER(dims);
    arg13 = new SpMatrix<bool> ((bool *)LOGICAL(xvec),INTEGER(ivec),pB,pE,idims[0],idims[1],LENGTH(xvec));
    
    /*@SWIG@*/
  }
  {
    SEXP rvec=own_variables;
    if (TYPEOF(rvec) != INTSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected int Vector as argument %d",14); return R_NilValue;
      };
    }
    
    arg14 = new Vector<int>((int*) INTEGER(rvec), LENGTH(rvec));
  }
  {
    SEXP rvec=N_own_variables;
    if (TYPEOF(rvec) != INTSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected int Vector as argument %d",15); return R_NilValue;
      };
    }
    
    arg15 = new Vector<int>((int*) INTEGER(rvec), LENGTH(rvec));
  }
  arg16 = static_cast< int >(INTEGER(num_threads)[0]);
  arg17 = static_cast< double >(REAL(tol)[0]);
  arg18 = LOGICAL(fixed_step)[0] ? true : false;
  arg19 = LOGICAL(ista)[0] ? true : false;
  arg20 = static_cast< int >(INTEGER(batch_size)[0]);
  arg21 = static_cast< int >(INTEGER(K)[0]);
  arg22 = static_cast< double >(REAL(lambda1)[0]);
  arg23 = static_cast< double >(REAL(lambda2)[0]);
  arg24 = static_cast< double >(REAL(lambda3)[0]);
  arg25 = static_cast< int >(INTEGER(iter)[0]);
  arg26 = static_cast< double >(REAL(t0)[0]);
  ecode27 = SWIG_AsVal_int(mode, &val27);
  if (!SWIG_IsOK(ecode27)) {
    SWIG_exception_fail(SWIG_ArgError(ecode27), "in method '" "alltrainDL" "', argument " "27"" of type '" "constraint_type""'");
  } 
  arg27 = static_cast< constraint_type >(val27);
  arg28 = reinterpret_cast< char * >(SWIG_strdup(CHAR(STRING_ELT(name_regul, 0))));
  arg29 = LOGICAL(posAlpha)[0] ? true : false;
  arg30 = LOGICAL(posD)[0] ? true : false;
  arg31 = LOGICAL(expand)[0] ? true : false;
  ecode32 = SWIG_AsVal_int(modeD, &val32);
  if (!SWIG_IsOK(ecode32)) {
    SWIG_exception_fail(SWIG_ArgError(ecode32), "in method '" "alltrainDL" "', argument " "32"" of type '" "constraint_type_D""'");
  } 
  arg32 = static_cast< constraint_type_D >(val32);
  arg33 = LOGICAL(whiten)[0] ? true : false;
  arg34 = LOGICAL(clean)[0] ? true : false;
  arg35 = LOGICAL(verbose)[0] ? true : false;
  arg36 = static_cast< double >(REAL(gamma1)[0]);
  arg37 = static_cast< double >(REAL(gamma2)[0]);
  arg38 = static_cast< double >(REAL(rho)[0]);
  arg39 = static_cast< int >(INTEGER(iter_updateD)[0]);
  arg40 = LOGICAL(stochastic)[0] ? true : false;
  arg41 = static_cast< int >(INTEGER(modeParam)[0]);
  arg42 = LOGICAL(batch)[0] ? true : false;
  arg43 = LOGICAL(log)[0] ? true : false;
  arg44 = reinterpret_cast< char * >(SWIG_strdup(CHAR(STRING_ELT(logName, 0))));
  try {
    result = (Matrix< double > *)_alltrainDL< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9,arg10,arg11,arg12,arg13,arg14,arg15,arg16,arg17,arg18,arg19,arg20,arg21,arg22,arg23,arg24,arg25,arg26,arg27,arg28,arg29,arg30,arg31,arg32,arg33,arg34,arg35,arg36,arg37,arg38,arg39,arg40,arg41,arg42,arg43,arg44);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t m = result->m();
    R_len_t n = result->n();
    double *data = result->rawX();
    SEXP rmat;
    PROTECT(rmat = Rf_allocMatrix(REALSXP,m,n));
    if (!rmat) error_return("Cannot alloc R matrix for return value");
    double *rdata = (double *)REAL(rmat);
    memcpy(rdata,data,m * n * sizeof(double));
    delete result;
    r_ans = rmat;
    UNPROTECT(1);
    
  }
  Rf_protect(r_ans);
  Rf_protect(R_OutputValues = Rf_allocVector(VECSXP,4));
  r_nprotect += 2;
  SET_VECTOR_ELT(R_OutputValues, 0, r_ans);
  r_ans = R_OutputValues;
  {
    //# test argout
    if(data_temp3 != NULL) {
      R_len_t m = data_temp3->m();
      R_len_t n = data_temp3->n();
      double *data = data_temp3->rawX();
      SEXP rmat;
      PROTECT(rmat = Rf_allocMatrix(REALSXP,m,n));
      if (!rmat) {
        Rf_error("Cannot alloc R matrix for arg %d",3); return R_NilValue;
      };
      double *rdata = (double *)REAL(rmat);
      memcpy(rdata,data,m * n * sizeof(double));
      delete data_temp3;
      r_ans = appendOutput(rmat, R_OutputValues);;
      UNPROTECT(1);
    }
  }
  {
    //# test argout
    if(data_temp4 != NULL) {
      R_len_t m = data_temp4->m();
      R_len_t n = data_temp4->n();
      double *data = data_temp4->rawX();
      SEXP rmat;
      PROTECT(rmat = Rf_allocMatrix(REALSXP,m,n));
      if (!rmat) {
        Rf_error("Cannot alloc R matrix for arg %d",4); return R_NilValue;
      };
      double *rdata = (double *)REAL(rmat);
      memcpy(rdata,data,m * n * sizeof(double));
      delete data_temp4;
      r_ans = appendOutput(rmat, R_OutputValues);;
      UNPROTECT(1);
    }
  }
  {
    //# test argout
    if(data_temp5 != NULL) {
      R_len_t n = data_temp5->n();
      int *data = data_temp5->rawX();
      SEXP rmat;
      PROTECT(rmat = Rf_allocMatrix(INTSXP,1,n));
      if (!rmat) {
        Rf_error("Cannot alloc R matrix for arg %d",5); return R_NilValue;
      };
      int *rdata = (int *)INTEGER(rmat);
      memcpy(rdata,data,n * sizeof(int));
      delete data_temp5;
      r_ans = appendOutput(rmat, R_OutputValues);;
      UNPROTECT(1);
    }
  }
  {
    delete arg1;
  }
  
  
  
  
  
  {
    delete arg7;
  }
  {
    delete arg8;
  }
  
  {
    delete arg10;
  }
  {
    delete arg11;
  }
  {
    delete arg12;
  }
  {
    delete arg13;
  }
  {
    delete arg14;
  }
  {
    delete arg15;
  }
  
  
  
  
  
  
  
  
  
  
  
  
  free(arg28);
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  free(arg44);
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_archetypalAnalysis ( SEXP X, SEXP p, SEXP robust, SEXP epsilon, SEXP computeXtX, SEXP stepsFISTA, SEXP stepsAS, SEXP randominit, SEXP numThreads)
{
  Matrix< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  int arg2 ;
  SpMatrix< double > **arg3 = (SpMatrix< double > **) 0 ;
  SpMatrix< double > **arg4 = (SpMatrix< double > **) 0 ;
  bool arg5 ;
  double arg6 ;
  bool arg7 ;
  int arg8 ;
  int arg9 ;
  bool arg10 ;
  int arg11 ;
  SpMatrix< double > *data_temp3 ;
  SpMatrix< double > *data_temp4 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  SEXP R_OutputValues;
  
  {
    arg3 = &data_temp3;
  }
  {
    arg4 = &data_temp4;
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  arg2 = static_cast< int >(INTEGER(p)[0]);
  arg5 = LOGICAL(robust)[0] ? true : false;
  arg6 = static_cast< double >(REAL(epsilon)[0]);
  arg7 = LOGICAL(computeXtX)[0] ? true : false;
  arg8 = static_cast< int >(INTEGER(stepsFISTA)[0]);
  arg9 = static_cast< int >(INTEGER(stepsAS)[0]);
  arg10 = LOGICAL(randominit)[0] ? true : false;
  arg11 = static_cast< int >(INTEGER(numThreads)[0]);
  try {
    result = (Matrix< double > *)_archetypalAnalysis< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9,arg10,arg11);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t m = result->m();
    R_len_t n = result->n();
    double *data = result->rawX();
    SEXP rmat;
    PROTECT(rmat = Rf_allocMatrix(REALSXP,m,n));
    if (!rmat) error_return("Cannot alloc R matrix for return value");
    double *rdata = (double *)REAL(rmat);
    memcpy(rdata,data,m * n * sizeof(double));
    delete result;
    r_ans = rmat;
    UNPROTECT(1);
    
  }
  Rf_protect(r_ans);
  Rf_protect(R_OutputValues = Rf_allocVector(VECSXP,3));
  r_nprotect += 2;
  SET_VECTOR_ELT(R_OutputValues, 0, r_ans);
  r_ans = R_OutputValues;
  {
    if(data_temp3 != NULL) {
      R_len_t m = data_temp3->m();
      R_len_t n = data_temp3->n();
      R_len_t nzmax = data_temp3->nzmax();
      SEXP indptr, indices, vdata, dims, output;
      PROTECT(indptr = Rf_allocVector(INTSXP,n + 1));
      PROTECT(indices = Rf_allocVector(INTSXP,nzmax));
      PROTECT(vdata = Rf_allocVector(REALSXP,nzmax));
      PROTECT(dims = Rf_allocVector(VECSXP,2));
      SET_VECTOR_ELT(dims,0,Rf_ScalarInteger(m));
      SET_VECTOR_ELT(dims,1,Rf_ScalarInteger(n));
      
      double *xdata = data_temp3->v();
      memcpy(REAL(vdata),xdata,nzmax * sizeof(double));
      int *pB = data_temp3->pB();
      int *r = data_temp3->r();
      memcpy(INTEGER(indices),r,nzmax * sizeof(int));
      memcpy(INTEGER(indptr),pB,(n + 1) * sizeof(int));
      delete data_temp3;
      PROTECT(output = Rf_allocVector(VECSXP,4));
      SET_VECTOR_ELT(output,0,indptr);
      SET_VECTOR_ELT(output,1,indices);
      SET_VECTOR_ELT(output,2,vdata);
      SET_VECTOR_ELT(output,3,dims);
      r_ans = appendOutput(output, R_OutputValues);;
      UNPROTECT(5);
    }
  }
  {
    if(data_temp4 != NULL) {
      R_len_t m = data_temp4->m();
      R_len_t n = data_temp4->n();
      R_len_t nzmax = data_temp4->nzmax();
      SEXP indptr, indices, vdata, dims, output;
      PROTECT(indptr = Rf_allocVector(INTSXP,n + 1));
      PROTECT(indices = Rf_allocVector(INTSXP,nzmax));
      PROTECT(vdata = Rf_allocVector(REALSXP,nzmax));
      PROTECT(dims = Rf_allocVector(VECSXP,2));
      SET_VECTOR_ELT(dims,0,Rf_ScalarInteger(m));
      SET_VECTOR_ELT(dims,1,Rf_ScalarInteger(n));
      
      double *xdata = data_temp4->v();
      memcpy(REAL(vdata),xdata,nzmax * sizeof(double));
      int *pB = data_temp4->pB();
      int *r = data_temp4->r();
      memcpy(INTEGER(indices),r,nzmax * sizeof(int));
      memcpy(INTEGER(indptr),pB,(n + 1) * sizeof(int));
      delete data_temp4;
      PROTECT(output = Rf_allocVector(VECSXP,4));
      SET_VECTOR_ELT(output,0,indptr);
      SET_VECTOR_ELT(output,1,indices);
      SET_VECTOR_ELT(output,2,vdata);
      SET_VECTOR_ELT(output,3,dims);
      r_ans = appendOutput(output, R_OutputValues);;
      UNPROTECT(5);
    }
  }
  {
    delete arg1;
  }
  
  
  
  
  
  
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_archetypalAnalysisInit ( SEXP X, SEXP Z0, SEXP robust, SEXP epsilon, SEXP computeXtX, SEXP stepsFISTA, SEXP stepsAS, SEXP numThreads)
{
  Matrix< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  SpMatrix< double > **arg3 = (SpMatrix< double > **) 0 ;
  SpMatrix< double > **arg4 = (SpMatrix< double > **) 0 ;
  bool arg5 ;
  double arg6 ;
  bool arg7 ;
  int arg8 ;
  int arg9 ;
  int arg10 ;
  SpMatrix< double > *data_temp3 ;
  SpMatrix< double > *data_temp4 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  SEXP R_OutputValues;
  
  {
    arg3 = &data_temp3;
  }
  {
    arg4 = &data_temp4;
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=Z0;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  arg5 = LOGICAL(robust)[0] ? true : false;
  arg6 = static_cast< double >(REAL(epsilon)[0]);
  arg7 = LOGICAL(computeXtX)[0] ? true : false;
  arg8 = static_cast< int >(INTEGER(stepsFISTA)[0]);
  arg9 = static_cast< int >(INTEGER(stepsAS)[0]);
  arg10 = static_cast< int >(INTEGER(numThreads)[0]);
  try {
    result = (Matrix< double > *)_archetypalAnalysisInit< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9,arg10);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t m = result->m();
    R_len_t n = result->n();
    double *data = result->rawX();
    SEXP rmat;
    PROTECT(rmat = Rf_allocMatrix(REALSXP,m,n));
    if (!rmat) error_return("Cannot alloc R matrix for return value");
    double *rdata = (double *)REAL(rmat);
    memcpy(rdata,data,m * n * sizeof(double));
    delete result;
    r_ans = rmat;
    UNPROTECT(1);
    
  }
  Rf_protect(r_ans);
  Rf_protect(R_OutputValues = Rf_allocVector(VECSXP,3));
  r_nprotect += 2;
  SET_VECTOR_ELT(R_OutputValues, 0, r_ans);
  r_ans = R_OutputValues;
  {
    if(data_temp3 != NULL) {
      R_len_t m = data_temp3->m();
      R_len_t n = data_temp3->n();
      R_len_t nzmax = data_temp3->nzmax();
      SEXP indptr, indices, vdata, dims, output;
      PROTECT(indptr = Rf_allocVector(INTSXP,n + 1));
      PROTECT(indices = Rf_allocVector(INTSXP,nzmax));
      PROTECT(vdata = Rf_allocVector(REALSXP,nzmax));
      PROTECT(dims = Rf_allocVector(VECSXP,2));
      SET_VECTOR_ELT(dims,0,Rf_ScalarInteger(m));
      SET_VECTOR_ELT(dims,1,Rf_ScalarInteger(n));
      
      double *xdata = data_temp3->v();
      memcpy(REAL(vdata),xdata,nzmax * sizeof(double));
      int *pB = data_temp3->pB();
      int *r = data_temp3->r();
      memcpy(INTEGER(indices),r,nzmax * sizeof(int));
      memcpy(INTEGER(indptr),pB,(n + 1) * sizeof(int));
      delete data_temp3;
      PROTECT(output = Rf_allocVector(VECSXP,4));
      SET_VECTOR_ELT(output,0,indptr);
      SET_VECTOR_ELT(output,1,indices);
      SET_VECTOR_ELT(output,2,vdata);
      SET_VECTOR_ELT(output,3,dims);
      r_ans = appendOutput(output, R_OutputValues);;
      UNPROTECT(5);
    }
  }
  {
    if(data_temp4 != NULL) {
      R_len_t m = data_temp4->m();
      R_len_t n = data_temp4->n();
      R_len_t nzmax = data_temp4->nzmax();
      SEXP indptr, indices, vdata, dims, output;
      PROTECT(indptr = Rf_allocVector(INTSXP,n + 1));
      PROTECT(indices = Rf_allocVector(INTSXP,nzmax));
      PROTECT(vdata = Rf_allocVector(REALSXP,nzmax));
      PROTECT(dims = Rf_allocVector(VECSXP,2));
      SET_VECTOR_ELT(dims,0,Rf_ScalarInteger(m));
      SET_VECTOR_ELT(dims,1,Rf_ScalarInteger(n));
      
      double *xdata = data_temp4->v();
      memcpy(REAL(vdata),xdata,nzmax * sizeof(double));
      int *pB = data_temp4->pB();
      int *r = data_temp4->r();
      memcpy(INTEGER(indices),r,nzmax * sizeof(int));
      memcpy(INTEGER(indptr),pB,(n + 1) * sizeof(int));
      delete data_temp4;
      PROTECT(output = Rf_allocVector(VECSXP,4));
      SET_VECTOR_ELT(output,0,indptr);
      SET_VECTOR_ELT(output,1,indices);
      SET_VECTOR_ELT(output,2,vdata);
      SET_VECTOR_ELT(output,3,dims);
      r_ans = appendOutput(output, R_OutputValues);;
      UNPROTECT(5);
    }
  }
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  
  
  
  
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_decompSimplex ( SEXP X, SEXP Z, SEXP computeXtX, SEXP numThreads)
{
  SpMatrix< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  bool arg3 ;
  int arg4 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=Z;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  arg3 = LOGICAL(computeXtX)[0] ? true : false;
  arg4 = static_cast< int >(INTEGER(numThreads)[0]);
  try {
    result = (SpMatrix< double > *)_decompSimplex< double >(arg1,arg2,arg3,arg4);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t m = result->m();
    R_len_t n = result->n();
    R_len_t nzmax = result->nzmax();
    SEXP indptr, indices, vdata, dims, output;
    PROTECT(indptr = Rf_allocVector(INTSXP,n + 1));
    PROTECT(indices = Rf_allocVector(INTSXP,nzmax));
    PROTECT(vdata = Rf_allocVector(REALSXP,nzmax));
    PROTECT(dims = Rf_allocVector(VECSXP,2));
    SET_VECTOR_ELT(dims,0,Rf_ScalarInteger(m));
    SET_VECTOR_ELT(dims,1,Rf_ScalarInteger(n));
    
    double *xdata = result->v();
    memcpy(REAL(vdata),xdata,nzmax * sizeof(double));
    int *pB = result->pB();
    int *r = result->r();
    memcpy(INTEGER(indices),r,nzmax * sizeof(int));
    memcpy(INTEGER(indptr),pB,(n + 1) * sizeof(int));
    
    PROTECT(output = Rf_allocVector(VECSXP,4));
    SET_VECTOR_ELT(output,0,indptr);
    SET_VECTOR_ELT(output,1,indices);
    SET_VECTOR_ELT(output,2,vdata);
    SET_VECTOR_ELT(output,3,dims);
    delete result;
    r_ans = output;
    UNPROTECT(5);
  }
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_fistaFlat ( SEXP X, SEXP D, SEXP alpha0, SEXP alpha, SEXP groups, SEXP num_threads, SEXP max_it, SEXP L0, SEXP fixed_step, SEXP gamma, SEXP s_lambda, SEXP delta, SEXP lambda2, SEXP lambda3, SEXP a, SEXP b, SEXP c, SEXP tol, SEXP it0, SEXP max_iter_backtracking, SEXP compute_gram, SEXP lin_admm, SEXP admm, SEXP intercept, SEXP resetflow, SEXP name_regul, SEXP name_loss, SEXP verbose, SEXP pos, SEXP clever, SEXP log, SEXP ista, SEXP subgrad, SEXP logName, SEXP is_inner_weights, SEXP inner_weights, SEXP size_group, SEXP sqrt_step, SEXP transpose, SEXP linesearch_mode)
{
  Matrix< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  AbstractMatrixB< double > *arg2 = (AbstractMatrixB< double > *) 0 ;
  Matrix< double > *arg3 = (Matrix< double > *) 0 ;
  Matrix< double > *arg4 = (Matrix< double > *) 0 ;
  Vector< int > *arg5 = (Vector< int > *) 0 ;
  int arg6 ;
  int arg7 ;
  double arg8 ;
  bool arg9 ;
  double arg10 ;
  double arg11 ;
  double arg12 ;
  double arg13 ;
  double arg14 ;
  double arg15 ;
  double arg16 ;
  double arg17 ;
  double arg18 ;
  int arg19 ;
  int arg20 ;
  bool arg21 ;
  bool arg22 ;
  bool arg23 ;
  bool arg24 ;
  bool arg25 ;
  char *arg26 = (char *) 0 ;
  char *arg27 = (char *) 0 ;
  bool arg28 ;
  bool arg29 ;
  bool arg30 ;
  bool arg31 ;
  bool arg32 ;
  bool arg33 ;
  char *arg34 = (char *) 0 ;
  bool arg35 ;
  Vector< double > *arg36 = (Vector< double > *) 0 ;
  int arg37 ;
  bool arg38 ;
  bool arg39 ;
  int arg40 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    SEXP pvec = Rf_getAttrib(D,Rf_install("p"));
    if(pvec ==  R_NilValue) {
      /*@SWIG:R_typemaps.i,113,map_matrix@*/
      SEXP rmat=D;
      SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
      if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
      {
        /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
        {
          Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
        };
      }
      arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
      
      /*@SWIG@*/
    } else {
      /*@SWIG:R_typemaps.i,227,map_sparse@*/
      SEXP spmat=D;
      SEXP ivec = Rf_getAttrib(spmat,Rf_install("i"));
      SEXP pvec = Rf_getAttrib(spmat,Rf_install("p"));
      SEXP xvec = Rf_getAttrib(spmat,Rf_install("x"));
      SEXP dims = Rf_getAttrib(spmat,Rf_install("Dim"));
      
      if (TYPEOF(xvec) != REALSXP || LENGTH(dims) != 2)
      {
        /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
        {
          Rf_error("Expected double sparse matrix as argument %d",2); return R_NilValue;
        };
      }
      int *pB = INTEGER(pvec);
      int *pE = pB + 1;
      int *idims = INTEGER(dims);
      arg2 = new SpMatrix<double> ((double *)REAL(xvec),INTEGER(ivec),pB,pE,idims[0],idims[1],LENGTH(xvec));
      
      /*@SWIG@*/
    }
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=alpha0;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",3); return R_NilValue;
      };
    }
    arg3 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=alpha;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",4); return R_NilValue;
      };
    }
    arg4 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    SEXP rvec=groups;
    if (TYPEOF(rvec) != INTSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected int Vector as argument %d",5); return R_NilValue;
      };
    }
    
    arg5 = new Vector<int>((int*) INTEGER(rvec), LENGTH(rvec));
  }
  arg6 = static_cast< int >(INTEGER(num_threads)[0]);
  arg7 = static_cast< int >(INTEGER(max_it)[0]);
  arg8 = static_cast< double >(REAL(L0)[0]);
  arg9 = LOGICAL(fixed_step)[0] ? true : false;
  arg10 = static_cast< double >(REAL(gamma)[0]);
  arg11 = static_cast< double >(REAL(s_lambda)[0]);
  arg12 = static_cast< double >(REAL(delta)[0]);
  arg13 = static_cast< double >(REAL(lambda2)[0]);
  arg14 = static_cast< double >(REAL(lambda3)[0]);
  arg15 = static_cast< double >(REAL(a)[0]);
  arg16 = static_cast< double >(REAL(b)[0]);
  arg17 = static_cast< double >(REAL(c)[0]);
  arg18 = static_cast< double >(REAL(tol)[0]);
  arg19 = static_cast< int >(INTEGER(it0)[0]);
  arg20 = static_cast< int >(INTEGER(max_iter_backtracking)[0]);
  arg21 = LOGICAL(compute_gram)[0] ? true : false;
  arg22 = LOGICAL(lin_admm)[0] ? true : false;
  arg23 = LOGICAL(admm)[0] ? true : false;
  arg24 = LOGICAL(intercept)[0] ? true : false;
  arg25 = LOGICAL(resetflow)[0] ? true : false;
  arg26 = reinterpret_cast< char * >(SWIG_strdup(CHAR(STRING_ELT(name_regul, 0))));
  arg27 = reinterpret_cast< char * >(SWIG_strdup(CHAR(STRING_ELT(name_loss, 0))));
  arg28 = LOGICAL(verbose)[0] ? true : false;
  arg29 = LOGICAL(pos)[0] ? true : false;
  arg30 = LOGICAL(clever)[0] ? true : false;
  arg31 = LOGICAL(log)[0] ? true : false;
  arg32 = LOGICAL(ista)[0] ? true : false;
  arg33 = LOGICAL(subgrad)[0] ? true : false;
  arg34 = reinterpret_cast< char * >(SWIG_strdup(CHAR(STRING_ELT(logName, 0))));
  arg35 = LOGICAL(is_inner_weights)[0] ? true : false;
  {
    SEXP rvec=inner_weights;
    if (TYPEOF(rvec) != REALSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double Vector as argument %d",36); return R_NilValue;
      };
    }
    
    arg36 = new Vector<double>((double*) REAL(rvec), LENGTH(rvec));
  }
  arg37 = static_cast< int >(INTEGER(size_group)[0]);
  arg38 = LOGICAL(sqrt_step)[0] ? true : false;
  arg39 = LOGICAL(transpose)[0] ? true : false;
  arg40 = static_cast< int >(INTEGER(linesearch_mode)[0]);
  try {
    result = (Matrix< double > *)_fistaFlat< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9,arg10,arg11,arg12,arg13,arg14,arg15,arg16,arg17,arg18,arg19,arg20,arg21,arg22,arg23,arg24,arg25,arg26,arg27,arg28,arg29,arg30,arg31,arg32,arg33,arg34,arg35,arg36,arg37,arg38,arg39,arg40);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t m = result->m();
    R_len_t n = result->n();
    double *data = result->rawX();
    SEXP rmat;
    PROTECT(rmat = Rf_allocMatrix(REALSXP,m,n));
    if (!rmat) error_return("Cannot alloc R matrix for return value");
    double *rdata = (double *)REAL(rmat);
    memcpy(rdata,data,m * n * sizeof(double));
    delete result;
    r_ans = rmat;
    UNPROTECT(1);
    
  }
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  {
    delete arg3;
  }
  {
    delete arg4;
  }
  {
    delete arg5;
  }
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  free(arg26);
  free(arg27);
  
  
  
  
  
  
  free(arg34);
  
  {
    delete arg36;
  }
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_fistaTree ( SEXP X, SEXP D, SEXP alpha0, SEXP alpha, SEXP eta_g, SEXP groups, SEXP own_variables, SEXP N_own_variables, SEXP num_threads, SEXP max_it, SEXP L0, SEXP fixed_step, SEXP gamma, SEXP s_lambda, SEXP delta, SEXP lambda2, SEXP lambda3, SEXP a, SEXP b, SEXP c, SEXP tol, SEXP it0, SEXP max_iter_backtracking, SEXP compute_gram, SEXP lin_admm, SEXP admm, SEXP intercept, SEXP resetflow, SEXP name_regul, SEXP name_loss, SEXP verbose, SEXP pos, SEXP clever, SEXP log, SEXP ista, SEXP subgrad, SEXP logName, SEXP is_inner_weights, SEXP inner_weights, SEXP size_group, SEXP sqrt_step, SEXP transpose, SEXP linesearch_mode)
{
  Matrix< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  AbstractMatrixB< double > *arg2 = (AbstractMatrixB< double > *) 0 ;
  Matrix< double > *arg3 = (Matrix< double > *) 0 ;
  Matrix< double > *arg4 = (Matrix< double > *) 0 ;
  Vector< double > *arg5 = (Vector< double > *) 0 ;
  SpMatrix< bool > *arg6 = (SpMatrix< bool > *) 0 ;
  Vector< int > *arg7 = (Vector< int > *) 0 ;
  Vector< int > *arg8 = (Vector< int > *) 0 ;
  int arg9 ;
  int arg10 ;
  double arg11 ;
  bool arg12 ;
  double arg13 ;
  double arg14 ;
  double arg15 ;
  double arg16 ;
  double arg17 ;
  double arg18 ;
  double arg19 ;
  double arg20 ;
  double arg21 ;
  int arg22 ;
  int arg23 ;
  bool arg24 ;
  bool arg25 ;
  bool arg26 ;
  bool arg27 ;
  bool arg28 ;
  char *arg29 = (char *) 0 ;
  char *arg30 = (char *) 0 ;
  bool arg31 ;
  bool arg32 ;
  bool arg33 ;
  bool arg34 ;
  bool arg35 ;
  bool arg36 ;
  char *arg37 = (char *) 0 ;
  bool arg38 ;
  Vector< double > *arg39 = (Vector< double > *) 0 ;
  int arg40 ;
  bool arg41 ;
  bool arg42 ;
  int arg43 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    SEXP pvec = Rf_getAttrib(D,Rf_install("p"));
    if(pvec ==  R_NilValue) {
      /*@SWIG:R_typemaps.i,113,map_matrix@*/
      SEXP rmat=D;
      SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
      if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
      {
        /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
        {
          Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
        };
      }
      arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
      
      /*@SWIG@*/
    } else {
      /*@SWIG:R_typemaps.i,227,map_sparse@*/
      SEXP spmat=D;
      SEXP ivec = Rf_getAttrib(spmat,Rf_install("i"));
      SEXP pvec = Rf_getAttrib(spmat,Rf_install("p"));
      SEXP xvec = Rf_getAttrib(spmat,Rf_install("x"));
      SEXP dims = Rf_getAttrib(spmat,Rf_install("Dim"));
      
      if (TYPEOF(xvec) != REALSXP || LENGTH(dims) != 2)
      {
        /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
        {
          Rf_error("Expected double sparse matrix as argument %d",2); return R_NilValue;
        };
      }
      int *pB = INTEGER(pvec);
      int *pE = pB + 1;
      int *idims = INTEGER(dims);
      arg2 = new SpMatrix<double> ((double *)REAL(xvec),INTEGER(ivec),pB,pE,idims[0],idims[1],LENGTH(xvec));
      
      /*@SWIG@*/
    }
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=alpha0;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",3); return R_NilValue;
      };
    }
    arg3 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=alpha;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",4); return R_NilValue;
      };
    }
    arg4 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    SEXP rvec=eta_g;
    if (TYPEOF(rvec) != REALSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double Vector as argument %d",5); return R_NilValue;
      };
    }
    
    arg5 = new Vector<double>((double*) REAL(rvec), LENGTH(rvec));
  }
  {
    /*@SWIG:R_typemaps.i,227,map_sparse@*/
    SEXP spmat=groups;
    SEXP ivec = Rf_getAttrib(spmat,Rf_install("i"));
    SEXP pvec = Rf_getAttrib(spmat,Rf_install("p"));
    SEXP xvec = Rf_getAttrib(spmat,Rf_install("x"));
    SEXP dims = Rf_getAttrib(spmat,Rf_install("Dim"));
    
    if (TYPEOF(xvec) != LGLSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected bool sparse matrix as argument %d",6); return R_NilValue;
      };
    }
    int *pB = INTEGER(pvec);
    int *pE = pB + 1;
    int *idims = INTEGER(dims);
    arg6 = new SpMatrix<bool> ((bool *)LOGICAL(xvec),INTEGER(ivec),pB,pE,idims[0],idims[1],LENGTH(xvec));
    
    /*@SWIG@*/
  }
  {
    SEXP rvec=own_variables;
    if (TYPEOF(rvec) != INTSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected int Vector as argument %d",7); return R_NilValue;
      };
    }
    
    arg7 = new Vector<int>((int*) INTEGER(rvec), LENGTH(rvec));
  }
  {
    SEXP rvec=N_own_variables;
    if (TYPEOF(rvec) != INTSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected int Vector as argument %d",8); return R_NilValue;
      };
    }
    
    arg8 = new Vector<int>((int*) INTEGER(rvec), LENGTH(rvec));
  }
  arg9 = static_cast< int >(INTEGER(num_threads)[0]);
  arg10 = static_cast< int >(INTEGER(max_it)[0]);
  arg11 = static_cast< double >(REAL(L0)[0]);
  arg12 = LOGICAL(fixed_step)[0] ? true : false;
  arg13 = static_cast< double >(REAL(gamma)[0]);
  arg14 = static_cast< double >(REAL(s_lambda)[0]);
  arg15 = static_cast< double >(REAL(delta)[0]);
  arg16 = static_cast< double >(REAL(lambda2)[0]);
  arg17 = static_cast< double >(REAL(lambda3)[0]);
  arg18 = static_cast< double >(REAL(a)[0]);
  arg19 = static_cast< double >(REAL(b)[0]);
  arg20 = static_cast< double >(REAL(c)[0]);
  arg21 = static_cast< double >(REAL(tol)[0]);
  arg22 = static_cast< int >(INTEGER(it0)[0]);
  arg23 = static_cast< int >(INTEGER(max_iter_backtracking)[0]);
  arg24 = LOGICAL(compute_gram)[0] ? true : false;
  arg25 = LOGICAL(lin_admm)[0] ? true : false;
  arg26 = LOGICAL(admm)[0] ? true : false;
  arg27 = LOGICAL(intercept)[0] ? true : false;
  arg28 = LOGICAL(resetflow)[0] ? true : false;
  arg29 = reinterpret_cast< char * >(SWIG_strdup(CHAR(STRING_ELT(name_regul, 0))));
  arg30 = reinterpret_cast< char * >(SWIG_strdup(CHAR(STRING_ELT(name_loss, 0))));
  arg31 = LOGICAL(verbose)[0] ? true : false;
  arg32 = LOGICAL(pos)[0] ? true : false;
  arg33 = LOGICAL(clever)[0] ? true : false;
  arg34 = LOGICAL(log)[0] ? true : false;
  arg35 = LOGICAL(ista)[0] ? true : false;
  arg36 = LOGICAL(subgrad)[0] ? true : false;
  arg37 = reinterpret_cast< char * >(SWIG_strdup(CHAR(STRING_ELT(logName, 0))));
  arg38 = LOGICAL(is_inner_weights)[0] ? true : false;
  {
    SEXP rvec=inner_weights;
    if (TYPEOF(rvec) != REALSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double Vector as argument %d",39); return R_NilValue;
      };
    }
    
    arg39 = new Vector<double>((double*) REAL(rvec), LENGTH(rvec));
  }
  arg40 = static_cast< int >(INTEGER(size_group)[0]);
  arg41 = LOGICAL(sqrt_step)[0] ? true : false;
  arg42 = LOGICAL(transpose)[0] ? true : false;
  arg43 = static_cast< int >(INTEGER(linesearch_mode)[0]);
  try {
    result = (Matrix< double > *)_fistaTree< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9,arg10,arg11,arg12,arg13,arg14,arg15,arg16,arg17,arg18,arg19,arg20,arg21,arg22,arg23,arg24,arg25,arg26,arg27,arg28,arg29,arg30,arg31,arg32,arg33,arg34,arg35,arg36,arg37,arg38,arg39,arg40,arg41,arg42,arg43);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t m = result->m();
    R_len_t n = result->n();
    double *data = result->rawX();
    SEXP rmat;
    PROTECT(rmat = Rf_allocMatrix(REALSXP,m,n));
    if (!rmat) error_return("Cannot alloc R matrix for return value");
    double *rdata = (double *)REAL(rmat);
    memcpy(rdata,data,m * n * sizeof(double));
    delete result;
    r_ans = rmat;
    UNPROTECT(1);
    
  }
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  {
    delete arg3;
  }
  {
    delete arg4;
  }
  {
    delete arg5;
  }
  {
    delete arg6;
  }
  {
    delete arg7;
  }
  {
    delete arg8;
  }
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  free(arg29);
  free(arg30);
  
  
  
  
  
  
  free(arg37);
  
  {
    delete arg39;
  }
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_fistaGraph ( SEXP X, SEXP D, SEXP alpha0, SEXP alpha, SEXP eta_g, SEXP groups, SEXP groups_var, SEXP num_threads, SEXP max_it, SEXP L0, SEXP fixed_step, SEXP gamma, SEXP s_lambda, SEXP delta, SEXP lambda2, SEXP lambda3, SEXP a, SEXP b, SEXP c, SEXP tol, SEXP it0, SEXP max_iter_backtracking, SEXP compute_gram, SEXP lin_admm, SEXP admm, SEXP intercept, SEXP resetflow, SEXP name_regul, SEXP name_loss, SEXP verbose, SEXP pos, SEXP clever, SEXP log, SEXP ista, SEXP subgrad, SEXP logName, SEXP is_inner_weights, SEXP inner_weights, SEXP size_group, SEXP sqrt_step, SEXP transpose, SEXP linesearch_mode)
{
  Matrix< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  AbstractMatrixB< double > *arg2 = (AbstractMatrixB< double > *) 0 ;
  Matrix< double > *arg3 = (Matrix< double > *) 0 ;
  Matrix< double > *arg4 = (Matrix< double > *) 0 ;
  Vector< double > *arg5 = (Vector< double > *) 0 ;
  SpMatrix< bool > *arg6 = (SpMatrix< bool > *) 0 ;
  SpMatrix< bool > *arg7 = (SpMatrix< bool > *) 0 ;
  int arg8 ;
  int arg9 ;
  double arg10 ;
  bool arg11 ;
  double arg12 ;
  double arg13 ;
  double arg14 ;
  double arg15 ;
  double arg16 ;
  double arg17 ;
  double arg18 ;
  double arg19 ;
  double arg20 ;
  int arg21 ;
  int arg22 ;
  bool arg23 ;
  bool arg24 ;
  bool arg25 ;
  bool arg26 ;
  bool arg27 ;
  char *arg28 = (char *) 0 ;
  char *arg29 = (char *) 0 ;
  bool arg30 ;
  bool arg31 ;
  bool arg32 ;
  bool arg33 ;
  bool arg34 ;
  bool arg35 ;
  char *arg36 = (char *) 0 ;
  bool arg37 ;
  Vector< double > *arg38 = (Vector< double > *) 0 ;
  int arg39 ;
  bool arg40 ;
  bool arg41 ;
  int arg42 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=X;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    SEXP pvec = Rf_getAttrib(D,Rf_install("p"));
    if(pvec ==  R_NilValue) {
      /*@SWIG:R_typemaps.i,113,map_matrix@*/
      SEXP rmat=D;
      SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
      if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
      {
        /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
        {
          Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
        };
      }
      arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
      
      /*@SWIG@*/
    } else {
      /*@SWIG:R_typemaps.i,227,map_sparse@*/
      SEXP spmat=D;
      SEXP ivec = Rf_getAttrib(spmat,Rf_install("i"));
      SEXP pvec = Rf_getAttrib(spmat,Rf_install("p"));
      SEXP xvec = Rf_getAttrib(spmat,Rf_install("x"));
      SEXP dims = Rf_getAttrib(spmat,Rf_install("Dim"));
      
      if (TYPEOF(xvec) != REALSXP || LENGTH(dims) != 2)
      {
        /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
        {
          Rf_error("Expected double sparse matrix as argument %d",2); return R_NilValue;
        };
      }
      int *pB = INTEGER(pvec);
      int *pE = pB + 1;
      int *idims = INTEGER(dims);
      arg2 = new SpMatrix<double> ((double *)REAL(xvec),INTEGER(ivec),pB,pE,idims[0],idims[1],LENGTH(xvec));
      
      /*@SWIG@*/
    }
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=alpha0;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",3); return R_NilValue;
      };
    }
    arg3 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=alpha;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",4); return R_NilValue;
      };
    }
    arg4 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    SEXP rvec=eta_g;
    if (TYPEOF(rvec) != REALSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double Vector as argument %d",5); return R_NilValue;
      };
    }
    
    arg5 = new Vector<double>((double*) REAL(rvec), LENGTH(rvec));
  }
  {
    /*@SWIG:R_typemaps.i,227,map_sparse@*/
    SEXP spmat=groups;
    SEXP ivec = Rf_getAttrib(spmat,Rf_install("i"));
    SEXP pvec = Rf_getAttrib(spmat,Rf_install("p"));
    SEXP xvec = Rf_getAttrib(spmat,Rf_install("x"));
    SEXP dims = Rf_getAttrib(spmat,Rf_install("Dim"));
    
    if (TYPEOF(xvec) != LGLSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected bool sparse matrix as argument %d",6); return R_NilValue;
      };
    }
    int *pB = INTEGER(pvec);
    int *pE = pB + 1;
    int *idims = INTEGER(dims);
    arg6 = new SpMatrix<bool> ((bool *)LOGICAL(xvec),INTEGER(ivec),pB,pE,idims[0],idims[1],LENGTH(xvec));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,227,map_sparse@*/
    SEXP spmat=groups_var;
    SEXP ivec = Rf_getAttrib(spmat,Rf_install("i"));
    SEXP pvec = Rf_getAttrib(spmat,Rf_install("p"));
    SEXP xvec = Rf_getAttrib(spmat,Rf_install("x"));
    SEXP dims = Rf_getAttrib(spmat,Rf_install("Dim"));
    
    if (TYPEOF(xvec) != LGLSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected bool sparse matrix as argument %d",7); return R_NilValue;
      };
    }
    int *pB = INTEGER(pvec);
    int *pE = pB + 1;
    int *idims = INTEGER(dims);
    arg7 = new SpMatrix<bool> ((bool *)LOGICAL(xvec),INTEGER(ivec),pB,pE,idims[0],idims[1],LENGTH(xvec));
    
    /*@SWIG@*/
  }
  arg8 = static_cast< int >(INTEGER(num_threads)[0]);
  arg9 = static_cast< int >(INTEGER(max_it)[0]);
  arg10 = static_cast< double >(REAL(L0)[0]);
  arg11 = LOGICAL(fixed_step)[0] ? true : false;
  arg12 = static_cast< double >(REAL(gamma)[0]);
  arg13 = static_cast< double >(REAL(s_lambda)[0]);
  arg14 = static_cast< double >(REAL(delta)[0]);
  arg15 = static_cast< double >(REAL(lambda2)[0]);
  arg16 = static_cast< double >(REAL(lambda3)[0]);
  arg17 = static_cast< double >(REAL(a)[0]);
  arg18 = static_cast< double >(REAL(b)[0]);
  arg19 = static_cast< double >(REAL(c)[0]);
  arg20 = static_cast< double >(REAL(tol)[0]);
  arg21 = static_cast< int >(INTEGER(it0)[0]);
  arg22 = static_cast< int >(INTEGER(max_iter_backtracking)[0]);
  arg23 = LOGICAL(compute_gram)[0] ? true : false;
  arg24 = LOGICAL(lin_admm)[0] ? true : false;
  arg25 = LOGICAL(admm)[0] ? true : false;
  arg26 = LOGICAL(intercept)[0] ? true : false;
  arg27 = LOGICAL(resetflow)[0] ? true : false;
  arg28 = reinterpret_cast< char * >(SWIG_strdup(CHAR(STRING_ELT(name_regul, 0))));
  arg29 = reinterpret_cast< char * >(SWIG_strdup(CHAR(STRING_ELT(name_loss, 0))));
  arg30 = LOGICAL(verbose)[0] ? true : false;
  arg31 = LOGICAL(pos)[0] ? true : false;
  arg32 = LOGICAL(clever)[0] ? true : false;
  arg33 = LOGICAL(log)[0] ? true : false;
  arg34 = LOGICAL(ista)[0] ? true : false;
  arg35 = LOGICAL(subgrad)[0] ? true : false;
  arg36 = reinterpret_cast< char * >(SWIG_strdup(CHAR(STRING_ELT(logName, 0))));
  arg37 = LOGICAL(is_inner_weights)[0] ? true : false;
  {
    SEXP rvec=inner_weights;
    if (TYPEOF(rvec) != REALSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double Vector as argument %d",38); return R_NilValue;
      };
    }
    
    arg38 = new Vector<double>((double*) REAL(rvec), LENGTH(rvec));
  }
  arg39 = static_cast< int >(INTEGER(size_group)[0]);
  arg40 = LOGICAL(sqrt_step)[0] ? true : false;
  arg41 = LOGICAL(transpose)[0] ? true : false;
  arg42 = static_cast< int >(INTEGER(linesearch_mode)[0]);
  try {
    result = (Matrix< double > *)_fistaGraph< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9,arg10,arg11,arg12,arg13,arg14,arg15,arg16,arg17,arg18,arg19,arg20,arg21,arg22,arg23,arg24,arg25,arg26,arg27,arg28,arg29,arg30,arg31,arg32,arg33,arg34,arg35,arg36,arg37,arg38,arg39,arg40,arg41,arg42);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t m = result->m();
    R_len_t n = result->n();
    double *data = result->rawX();
    SEXP rmat;
    PROTECT(rmat = Rf_allocMatrix(REALSXP,m,n));
    if (!rmat) error_return("Cannot alloc R matrix for return value");
    double *rdata = (double *)REAL(rmat);
    memcpy(rdata,data,m * n * sizeof(double));
    delete result;
    r_ans = rmat;
    UNPROTECT(1);
    
  }
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  {
    delete arg3;
  }
  {
    delete arg4;
  }
  {
    delete arg5;
  }
  {
    delete arg6;
  }
  {
    delete arg7;
  }
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  free(arg28);
  free(arg29);
  
  
  
  
  
  
  free(arg36);
  
  {
    delete arg38;
  }
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_proximalFlat ( SEXP alpha0, SEXP alpha, SEXP groups, SEXP num_threads, SEXP lambda1, SEXP lambda2, SEXP lambda3, SEXP intercept, SEXP resetflow, SEXP name_regul, SEXP verbose, SEXP pos, SEXP clever, SEXP eval, SEXP size_group, SEXP transpose)
{
  Vector< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  Vector< int > *arg3 = (Vector< int > *) 0 ;
  int arg4 ;
  double arg5 ;
  double arg6 ;
  double arg7 ;
  bool arg8 ;
  bool arg9 ;
  char *arg10 = (char *) 0 ;
  bool arg11 ;
  bool arg12 ;
  bool arg13 ;
  bool arg14 ;
  int arg15 ;
  bool arg16 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=alpha0;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=alpha;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    SEXP rvec=groups;
    if (TYPEOF(rvec) != INTSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected int Vector as argument %d",3); return R_NilValue;
      };
    }
    
    arg3 = new Vector<int>((int*) INTEGER(rvec), LENGTH(rvec));
  }
  arg4 = static_cast< int >(INTEGER(num_threads)[0]);
  arg5 = static_cast< double >(REAL(lambda1)[0]);
  arg6 = static_cast< double >(REAL(lambda2)[0]);
  arg7 = static_cast< double >(REAL(lambda3)[0]);
  arg8 = LOGICAL(intercept)[0] ? true : false;
  arg9 = LOGICAL(resetflow)[0] ? true : false;
  arg10 = reinterpret_cast< char * >(SWIG_strdup(CHAR(STRING_ELT(name_regul, 0))));
  arg11 = LOGICAL(verbose)[0] ? true : false;
  arg12 = LOGICAL(pos)[0] ? true : false;
  arg13 = LOGICAL(clever)[0] ? true : false;
  arg14 = LOGICAL(eval)[0] ? true : false;
  arg15 = static_cast< int >(INTEGER(size_group)[0]);
  arg16 = LOGICAL(transpose)[0] ? true : false;
  try {
    result = (Vector< double > *)_proximalFlat< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9,arg10,arg11,arg12,arg13,arg14,arg15,arg16);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t n = result->n();
    double *data = result->rawX();
    SEXP rmat;
    PROTECT(rmat = Rf_allocMatrix(REALSXP,1,n));
    if (!rmat) error_return("Cannot alloc R matrix for return value");
    double *rdata = (double *)REAL(rmat);
    memcpy(rdata,data,n * sizeof(double));
    delete result;
    r_ans = rmat;
    UNPROTECT(1);
    
  }
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  {
    delete arg3;
  }
  
  
  
  
  
  
  free(arg10);
  
  
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_proximalTree ( SEXP alpha0, SEXP alpha, SEXP eta_g, SEXP groups, SEXP own_variables, SEXP N_own_variables, SEXP num_threads, SEXP lambda1, SEXP lambda2, SEXP lambda3, SEXP intercept, SEXP resetflow, SEXP name_regul, SEXP verbose, SEXP pos, SEXP clever, SEXP eval, SEXP size_group, SEXP transpose)
{
  Vector< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  Vector< double > *arg3 = (Vector< double > *) 0 ;
  SpMatrix< bool > *arg4 = (SpMatrix< bool > *) 0 ;
  Vector< int > *arg5 = (Vector< int > *) 0 ;
  Vector< int > *arg6 = (Vector< int > *) 0 ;
  int arg7 ;
  double arg8 ;
  double arg9 ;
  double arg10 ;
  bool arg11 ;
  bool arg12 ;
  char *arg13 = (char *) 0 ;
  bool arg14 ;
  bool arg15 ;
  bool arg16 ;
  bool arg17 ;
  int arg18 ;
  bool arg19 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=alpha0;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=alpha;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    SEXP rvec=eta_g;
    if (TYPEOF(rvec) != REALSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double Vector as argument %d",3); return R_NilValue;
      };
    }
    
    arg3 = new Vector<double>((double*) REAL(rvec), LENGTH(rvec));
  }
  {
    /*@SWIG:R_typemaps.i,227,map_sparse@*/
    SEXP spmat=groups;
    SEXP ivec = Rf_getAttrib(spmat,Rf_install("i"));
    SEXP pvec = Rf_getAttrib(spmat,Rf_install("p"));
    SEXP xvec = Rf_getAttrib(spmat,Rf_install("x"));
    SEXP dims = Rf_getAttrib(spmat,Rf_install("Dim"));
    
    if (TYPEOF(xvec) != LGLSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected bool sparse matrix as argument %d",4); return R_NilValue;
      };
    }
    int *pB = INTEGER(pvec);
    int *pE = pB + 1;
    int *idims = INTEGER(dims);
    arg4 = new SpMatrix<bool> ((bool *)LOGICAL(xvec),INTEGER(ivec),pB,pE,idims[0],idims[1],LENGTH(xvec));
    
    /*@SWIG@*/
  }
  {
    SEXP rvec=own_variables;
    if (TYPEOF(rvec) != INTSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected int Vector as argument %d",5); return R_NilValue;
      };
    }
    
    arg5 = new Vector<int>((int*) INTEGER(rvec), LENGTH(rvec));
  }
  {
    SEXP rvec=N_own_variables;
    if (TYPEOF(rvec) != INTSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected int Vector as argument %d",6); return R_NilValue;
      };
    }
    
    arg6 = new Vector<int>((int*) INTEGER(rvec), LENGTH(rvec));
  }
  arg7 = static_cast< int >(INTEGER(num_threads)[0]);
  arg8 = static_cast< double >(REAL(lambda1)[0]);
  arg9 = static_cast< double >(REAL(lambda2)[0]);
  arg10 = static_cast< double >(REAL(lambda3)[0]);
  arg11 = LOGICAL(intercept)[0] ? true : false;
  arg12 = LOGICAL(resetflow)[0] ? true : false;
  arg13 = reinterpret_cast< char * >(SWIG_strdup(CHAR(STRING_ELT(name_regul, 0))));
  arg14 = LOGICAL(verbose)[0] ? true : false;
  arg15 = LOGICAL(pos)[0] ? true : false;
  arg16 = LOGICAL(clever)[0] ? true : false;
  arg17 = LOGICAL(eval)[0] ? true : false;
  arg18 = static_cast< int >(INTEGER(size_group)[0]);
  arg19 = LOGICAL(transpose)[0] ? true : false;
  try {
    result = (Vector< double > *)_proximalTree< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9,arg10,arg11,arg12,arg13,arg14,arg15,arg16,arg17,arg18,arg19);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t n = result->n();
    double *data = result->rawX();
    SEXP rmat;
    PROTECT(rmat = Rf_allocMatrix(REALSXP,1,n));
    if (!rmat) error_return("Cannot alloc R matrix for return value");
    double *rdata = (double *)REAL(rmat);
    memcpy(rdata,data,n * sizeof(double));
    delete result;
    r_ans = rmat;
    UNPROTECT(1);
    
  }
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  {
    delete arg3;
  }
  {
    delete arg4;
  }
  {
    delete arg5;
  }
  {
    delete arg6;
  }
  
  
  
  
  
  
  free(arg13);
  
  
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_proximalGraph ( SEXP alpha0, SEXP alpha, SEXP eta_g, SEXP groups, SEXP groups_var, SEXP num_threads, SEXP lambda1, SEXP lambda2, SEXP lambda3, SEXP intercept, SEXP resetflow, SEXP name_regul, SEXP verbose, SEXP pos, SEXP clever, SEXP eval, SEXP size_group, SEXP transpose)
{
  Vector< double > *result = 0 ;
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  Vector< double > *arg3 = (Vector< double > *) 0 ;
  SpMatrix< bool > *arg4 = (SpMatrix< bool > *) 0 ;
  SpMatrix< bool > *arg5 = (SpMatrix< bool > *) 0 ;
  int arg6 ;
  double arg7 ;
  double arg8 ;
  double arg9 ;
  bool arg10 ;
  bool arg11 ;
  char *arg12 = (char *) 0 ;
  bool arg13 ;
  bool arg14 ;
  bool arg15 ;
  bool arg16 ;
  int arg17 ;
  bool arg18 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=alpha0;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=alpha;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    SEXP rvec=eta_g;
    if (TYPEOF(rvec) != REALSXP || ! Rf_isVector(rvec))
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double Vector as argument %d",3); return R_NilValue;
      };
    }
    
    arg3 = new Vector<double>((double*) REAL(rvec), LENGTH(rvec));
  }
  {
    /*@SWIG:R_typemaps.i,227,map_sparse@*/
    SEXP spmat=groups;
    SEXP ivec = Rf_getAttrib(spmat,Rf_install("i"));
    SEXP pvec = Rf_getAttrib(spmat,Rf_install("p"));
    SEXP xvec = Rf_getAttrib(spmat,Rf_install("x"));
    SEXP dims = Rf_getAttrib(spmat,Rf_install("Dim"));
    
    if (TYPEOF(xvec) != LGLSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected bool sparse matrix as argument %d",4); return R_NilValue;
      };
    }
    int *pB = INTEGER(pvec);
    int *pE = pB + 1;
    int *idims = INTEGER(dims);
    arg4 = new SpMatrix<bool> ((bool *)LOGICAL(xvec),INTEGER(ivec),pB,pE,idims[0],idims[1],LENGTH(xvec));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,227,map_sparse@*/
    SEXP spmat=groups_var;
    SEXP ivec = Rf_getAttrib(spmat,Rf_install("i"));
    SEXP pvec = Rf_getAttrib(spmat,Rf_install("p"));
    SEXP xvec = Rf_getAttrib(spmat,Rf_install("x"));
    SEXP dims = Rf_getAttrib(spmat,Rf_install("Dim"));
    
    if (TYPEOF(xvec) != LGLSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected bool sparse matrix as argument %d",5); return R_NilValue;
      };
    }
    int *pB = INTEGER(pvec);
    int *pE = pB + 1;
    int *idims = INTEGER(dims);
    arg5 = new SpMatrix<bool> ((bool *)LOGICAL(xvec),INTEGER(ivec),pB,pE,idims[0],idims[1],LENGTH(xvec));
    
    /*@SWIG@*/
  }
  arg6 = static_cast< int >(INTEGER(num_threads)[0]);
  arg7 = static_cast< double >(REAL(lambda1)[0]);
  arg8 = static_cast< double >(REAL(lambda2)[0]);
  arg9 = static_cast< double >(REAL(lambda3)[0]);
  arg10 = LOGICAL(intercept)[0] ? true : false;
  arg11 = LOGICAL(resetflow)[0] ? true : false;
  arg12 = reinterpret_cast< char * >(SWIG_strdup(CHAR(STRING_ELT(name_regul, 0))));
  arg13 = LOGICAL(verbose)[0] ? true : false;
  arg14 = LOGICAL(pos)[0] ? true : false;
  arg15 = LOGICAL(clever)[0] ? true : false;
  arg16 = LOGICAL(eval)[0] ? true : false;
  arg17 = static_cast< int >(INTEGER(size_group)[0]);
  arg18 = LOGICAL(transpose)[0] ? true : false;
  try {
    result = (Vector< double > *)_proximalGraph< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7,arg8,arg9,arg10,arg11,arg12,arg13,arg14,arg15,arg16,arg17,arg18);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t n = result->n();
    double *data = result->rawX();
    SEXP rmat;
    PROTECT(rmat = Rf_allocMatrix(REALSXP,1,n));
    if (!rmat) error_return("Cannot alloc R matrix for return value");
    double *rdata = (double *)REAL(rmat);
    memcpy(rdata,data,n * sizeof(double));
    delete result;
    r_ans = rmat;
    UNPROTECT(1);
    
  }
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  {
    delete arg3;
  }
  {
    delete arg4;
  }
  {
    delete arg5;
  }
  
  
  
  
  
  
  free(arg12);
  
  
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_simpleGroupTree ( SEXP degr, SEXP n)
{
  std::vector< StructNodeElem< double > * > *result = 0 ;
  int *arg1 = (int *) 0 ;
  int arg2 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    {
      int _rswigi;
      int _rswiglen = LENGTH(degr);
      arg1 = static_cast< int * >(calloc(sizeof(int), _rswiglen));
      for (_rswigi=0; _rswigi< _rswiglen; _rswigi++) {
        arg1[_rswigi] = INTEGER(degr)[_rswigi];
      }
    }
  }
  arg2 = static_cast< int >(INTEGER(n)[0]);
  try {
    result = (std::vector< StructNodeElem< double > * > *)_simpleGroupTree< double >(arg1,arg2);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    int n = result->size();
    SEXP node_list;
    PROTECT(node_list = Rf_allocVector(VECSXP,n));
    int i = 0;
    for(std::vector<StructNodeElem<double> *>::iterator it = result->begin();it != result->end();it++, i++) {
      SEXP vars, children, elem;
      PROTECT(elem = Rf_allocVector(VECSXP,4));
      StructNodeElem<double> *node = *it;
      int inode = node->node_num;
      SET_VECTOR_ELT(elem,0,Rf_ScalarInteger(inode));
      SET_VECTOR_ELT(elem,1,Rf_ScalarReal(node->weight));
      int k = node->vars->size();
      PROTECT(vars = Rf_allocVector(INTSXP,k));
      int * rvars = (int *)INTEGER(vars);
      memcpy(rvars,node->vars->data(),k * sizeof(int));
      SET_VECTOR_ELT(elem,2,vars);
      
      k = node->children->size();
      PROTECT(children = Rf_allocVector(INTSXP,k));
      int * rchildren = (int *)INTEGER(children);
      memcpy(rchildren,node->children->data(),k * sizeof(int));
      SET_VECTOR_ELT(elem,3,children);
      
      SET_VECTOR_ELT(node_list,i,elem);
      UNPROTECT(3);
    }
    del_gstruct(result);
    r_ans = node_list;
    UNPROTECT(1);
  }
  
  free(arg1);
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_readGroupStruct ( SEXP file)
{
  std::vector< StructNodeElem< double > * > *result = 0 ;
  char *arg1 = (char *) 0 ;
  int res1 ;
  char *buf1 = 0 ;
  int alloc1 = 0 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  res1 = SWIG_AsCharPtrAndSize(file, &buf1, NULL, &alloc1);
  if (!SWIG_IsOK(res1)) {
    SWIG_exception_fail(SWIG_ArgError(res1), "in method '" "readGroupStruct" "', argument " "1"" of type '" "char const *""'");
  }
  arg1 = reinterpret_cast< char * >(buf1);
  try {
    result = (std::vector< StructNodeElem< double > * > *)_readGroupStruct< double >((char const *)arg1);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    int n = result->size();
    SEXP node_list;
    PROTECT(node_list = Rf_allocVector(VECSXP,n));
    int i = 0;
    for(std::vector<StructNodeElem<double> *>::iterator it = result->begin();it != result->end();it++, i++) {
      SEXP vars, children, elem;
      PROTECT(elem = Rf_allocVector(VECSXP,4));
      StructNodeElem<double> *node = *it;
      int inode = node->node_num;
      SET_VECTOR_ELT(elem,0,Rf_ScalarInteger(inode));
      SET_VECTOR_ELT(elem,1,Rf_ScalarReal(node->weight));
      int k = node->vars->size();
      PROTECT(vars = Rf_allocVector(INTSXP,k));
      int * rvars = (int *)INTEGER(vars);
      memcpy(rvars,node->vars->data(),k * sizeof(int));
      SET_VECTOR_ELT(elem,2,vars);
      
      k = node->children->size();
      PROTECT(children = Rf_allocVector(INTSXP,k));
      int * rchildren = (int *)INTEGER(children);
      memcpy(rchildren,node->children->data(),k * sizeof(int));
      SET_VECTOR_ELT(elem,3,children);
      
      SET_VECTOR_ELT(node_list,i,elem);
      UNPROTECT(3);
    }
    del_gstruct(result);
    r_ans = node_list;
    UNPROTECT(1);
  }
  if (alloc1 == SWIG_NEWOBJ) delete[] buf1;
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_groupStructOfString ( SEXP data)
{
  std::vector< StructNodeElem< double > * > *result = 0 ;
  char *arg1 = (char *) 0 ;
  int res1 ;
  char *buf1 = 0 ;
  int alloc1 = 0 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  res1 = SWIG_AsCharPtrAndSize(data, &buf1, NULL, &alloc1);
  if (!SWIG_IsOK(res1)) {
    SWIG_exception_fail(SWIG_ArgError(res1), "in method '" "groupStructOfString" "', argument " "1"" of type '" "char const *""'");
  }
  arg1 = reinterpret_cast< char * >(buf1);
  try {
    result = (std::vector< StructNodeElem< double > * > *)_groupStructOfString< double >((char const *)arg1);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    int n = result->size();
    SEXP node_list;
    PROTECT(node_list = Rf_allocVector(VECSXP,n));
    int i = 0;
    for(std::vector<StructNodeElem<double> *>::iterator it = result->begin();it != result->end();it++, i++) {
      SEXP vars, children, elem;
      PROTECT(elem = Rf_allocVector(VECSXP,4));
      StructNodeElem<double> *node = *it;
      int inode = node->node_num;
      SET_VECTOR_ELT(elem,0,Rf_ScalarInteger(inode));
      SET_VECTOR_ELT(elem,1,Rf_ScalarReal(node->weight));
      int k = node->vars->size();
      PROTECT(vars = Rf_allocVector(INTSXP,k));
      int * rvars = (int *)INTEGER(vars);
      memcpy(rvars,node->vars->data(),k * sizeof(int));
      SET_VECTOR_ELT(elem,2,vars);
      
      k = node->children->size();
      PROTECT(children = Rf_allocVector(INTSXP,k));
      int * rchildren = (int *)INTEGER(children);
      memcpy(rchildren,node->children->data(),k * sizeof(int));
      SET_VECTOR_ELT(elem,3,children);
      
      SET_VECTOR_ELT(node_list,i,elem);
      UNPROTECT(3);
    }
    del_gstruct(result);
    r_ans = node_list;
    UNPROTECT(1);
  }
  if (alloc1 == SWIG_NEWOBJ) delete[] buf1;
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_graphOfGroupStruct ( SEXP gstruct)
{
  Vector< double > *result = 0 ;
  std::vector< StructNodeElem< double > * > *arg1 = (std::vector< StructNodeElem< double > * > *) 0 ;
  SpMatrix< bool > **arg2 = (SpMatrix< bool > **) 0 ;
  SpMatrix< bool > **arg3 = (SpMatrix< bool > **) 0 ;
  SpMatrix< bool > *data_temp2 ;
  SpMatrix< bool > *data_temp3 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  SEXP R_OutputValues;
  
  {
    arg2 = &data_temp2;
  }
  {
    arg3 = &data_temp3;
  }
  {
    SEXP rtree = gstruct;
    if(TYPEOF(rtree) != VECSXP || ! Rf_isVector(rtree)) {
      {
        Rf_error("Expected VECSXP Vector as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new std::vector<StructNodeElem<double> *>;
    int n = Rf_length(rtree);
    for (int i = 0;i < n;i++) {
      SEXP rnode = VECTOR_ELT(rtree,i);
      if(TYPEOF(rnode) != VECSXP || ! Rf_isVector(rnode)) {
        {
          Rf_error("Bad type for node elem in arg %d",1); return R_NilValue;
        };
      }
      if(Rf_length(rnode) != 4) {
        {
          Rf_error("A node must have 4 elements in arg %d",1); return R_NilValue;
        };
      }
      int inode = *((int *) INTEGER(VECTOR_ELT(rnode,0)));
      double w = *((double *)REAL(VECTOR_ELT(rnode,1)));
      std::vector<int> *vars = new std::vector<int>;
      std::vector<int> *children = new std::vector<int>;
      SEXP rvars = VECTOR_ELT(rnode,2);
      SEXP rchildren = VECTOR_ELT(rnode,3);
      int *pvars = (int *) INTEGER(rvars);
      int *pchildren = (int *) INTEGER(rchildren);
      for(int i = 0;i < Rf_length(rvars);i++)
      vars->push_back(*pvars++);
      for(int i = 0;i < Rf_length(rchildren);i++)
      children->push_back(*pchildren++);
      StructNodeElem<double> *node = new StructNodeElem<double>(inode,w,vars,children);
      arg1->push_back(node);
    }
  }
  try {
    result = (Vector< double > *)_graphOfGroupStruct< double >(arg1,arg2,arg3);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  {
    R_result_pos = 0;
    R_len_t n = result->n();
    double *data = result->rawX();
    SEXP rmat;
    PROTECT(rmat = Rf_allocMatrix(REALSXP,1,n));
    if (!rmat) error_return("Cannot alloc R matrix for return value");
    double *rdata = (double *)REAL(rmat);
    memcpy(rdata,data,n * sizeof(double));
    delete result;
    r_ans = rmat;
    UNPROTECT(1);
    
  }
  Rf_protect(r_ans);
  Rf_protect(R_OutputValues = Rf_allocVector(VECSXP,3));
  r_nprotect += 2;
  SET_VECTOR_ELT(R_OutputValues, 0, r_ans);
  r_ans = R_OutputValues;
  {
    if(data_temp2 != NULL) {
      R_len_t m = data_temp2->m();
      R_len_t n = data_temp2->n();
      R_len_t nzmax = data_temp2->nzmax();
      SEXP indptr, indices, vdata, dims, output;
      PROTECT(indptr = Rf_allocVector(INTSXP,n + 1));
      PROTECT(indices = Rf_allocVector(INTSXP,nzmax));
      PROTECT(vdata = Rf_allocVector(LGLSXP,nzmax));
      PROTECT(dims = Rf_allocVector(VECSXP,2));
      SET_VECTOR_ELT(dims,0,Rf_ScalarInteger(m));
      SET_VECTOR_ELT(dims,1,Rf_ScalarInteger(n));
      
      bool *xdata = data_temp2->v();
      memcpy(LOGICAL(vdata),xdata,nzmax * sizeof(bool));
      int *pB = data_temp2->pB();
      int *r = data_temp2->r();
      memcpy(INTEGER(indices),r,nzmax * sizeof(int));
      memcpy(INTEGER(indptr),pB,(n + 1) * sizeof(int));
      delete data_temp2;
      PROTECT(output = Rf_allocVector(VECSXP,4));
      SET_VECTOR_ELT(output,0,indptr);
      SET_VECTOR_ELT(output,1,indices);
      SET_VECTOR_ELT(output,2,vdata);
      SET_VECTOR_ELT(output,3,dims);
      r_ans = appendOutput(output, R_OutputValues);;
      UNPROTECT(5);
    }
  }
  {
    if(data_temp3 != NULL) {
      R_len_t m = data_temp3->m();
      R_len_t n = data_temp3->n();
      R_len_t nzmax = data_temp3->nzmax();
      SEXP indptr, indices, vdata, dims, output;
      PROTECT(indptr = Rf_allocVector(INTSXP,n + 1));
      PROTECT(indices = Rf_allocVector(INTSXP,nzmax));
      PROTECT(vdata = Rf_allocVector(LGLSXP,nzmax));
      PROTECT(dims = Rf_allocVector(VECSXP,2));
      SET_VECTOR_ELT(dims,0,Rf_ScalarInteger(m));
      SET_VECTOR_ELT(dims,1,Rf_ScalarInteger(n));
      
      bool *xdata = data_temp3->v();
      memcpy(LOGICAL(vdata),xdata,nzmax * sizeof(bool));
      int *pB = data_temp3->pB();
      int *r = data_temp3->r();
      memcpy(INTEGER(indices),r,nzmax * sizeof(int));
      memcpy(INTEGER(indptr),pB,(n + 1) * sizeof(int));
      delete data_temp3;
      PROTECT(output = Rf_allocVector(VECSXP,4));
      SET_VECTOR_ELT(output,0,indptr);
      SET_VECTOR_ELT(output,1,indices);
      SET_VECTOR_ELT(output,2,vdata);
      SET_VECTOR_ELT(output,3,dims);
      r_ans = appendOutput(output, R_OutputValues);;
      UNPROTECT(5);
    }
  }
  {
    del_gstruct(arg1);
  }
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_treeOfGroupStruct ( SEXP gstruct, SEXP s_swig_copy)
{
  int result;
  std::vector< StructNodeElem< double > * > *arg1 = (std::vector< StructNodeElem< double > * > *) 0 ;
  int **arg2 = (int **) 0 ;
  int *arg3 = (int *) 0 ;
  Vector< double > **arg4 = (Vector< double > **) 0 ;
  SpMatrix< bool > **arg5 = (SpMatrix< bool > **) 0 ;
  Vector< int > **arg6 = (Vector< int > **) 0 ;
  Vector< int > **arg7 = (Vector< int > **) 0 ;
  int *data_temp2 ;
  int dim_temp2 ;
  Vector< double > *data_temp4 ;
  SpMatrix< bool > *data_temp5 ;
  Vector< int > *data_temp6 ;
  Vector< int > *data_temp7 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  SEXP R_OutputValues;
  
  {
    arg2 = &data_temp2;
    arg3 = &dim_temp2;
  }
  {
    arg4 = &data_temp4;
  }
  {
    arg5 = &data_temp5;
  }
  {
    arg6 = &data_temp6;
  }
  {
    arg7 = &data_temp7;
  }
  {
    SEXP rtree = gstruct;
    if(TYPEOF(rtree) != VECSXP || ! Rf_isVector(rtree)) {
      {
        Rf_error("Expected VECSXP Vector as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new std::vector<StructNodeElem<double> *>;
    int n = Rf_length(rtree);
    for (int i = 0;i < n;i++) {
      SEXP rnode = VECTOR_ELT(rtree,i);
      if(TYPEOF(rnode) != VECSXP || ! Rf_isVector(rnode)) {
        {
          Rf_error("Bad type for node elem in arg %d",1); return R_NilValue;
        };
      }
      if(Rf_length(rnode) != 4) {
        {
          Rf_error("A node must have 4 elements in arg %d",1); return R_NilValue;
        };
      }
      int inode = *((int *) INTEGER(VECTOR_ELT(rnode,0)));
      double w = *((double *)REAL(VECTOR_ELT(rnode,1)));
      std::vector<int> *vars = new std::vector<int>;
      std::vector<int> *children = new std::vector<int>;
      SEXP rvars = VECTOR_ELT(rnode,2);
      SEXP rchildren = VECTOR_ELT(rnode,3);
      int *pvars = (int *) INTEGER(rvars);
      int *pchildren = (int *) INTEGER(rchildren);
      for(int i = 0;i < Rf_length(rvars);i++)
      vars->push_back(*pvars++);
      for(int i = 0;i < Rf_length(rchildren);i++)
      children->push_back(*pchildren++);
      StructNodeElem<double> *node = new StructNodeElem<double>(inode,w,vars,children);
      arg1->push_back(node);
    }
  }
  try {
    result = (int)_treeOfGroupStruct< double >(arg1,arg2,arg3,arg4,arg5,arg6,arg7);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  r_ans = Rf_ScalarInteger(result);
  Rf_protect(r_ans);
  Rf_protect(R_OutputValues = Rf_allocVector(VECSXP,6));
  r_nprotect += 2;
  SET_VECTOR_ELT(R_OutputValues, 0, r_ans);
  r_ans = R_OutputValues;
  {
    SEXP perm;
    if(data_temp2 != NULL) {
      int n = dim_temp2;
      PROTECT(perm = Rf_allocVector(INTSXP,n));
      int *pperm = INTEGER(perm);
      memcpy(pperm,data_temp2,n * sizeof(int));
      delete []data_temp2;
      UNPROTECT(1);
    } else perm = R_NilValue;
    r_ans = appendOutput(perm, R_OutputValues);;
  }
  {
    //# test argout
    if(data_temp4 != NULL) {
      R_len_t n = data_temp4->n();
      double *data = data_temp4->rawX();
      SEXP rmat;
      PROTECT(rmat = Rf_allocMatrix(REALSXP,1,n));
      if (!rmat) {
        Rf_error("Cannot alloc R matrix for arg %d",4); return R_NilValue;
      };
      double *rdata = (double *)REAL(rmat);
      memcpy(rdata,data,n * sizeof(double));
      delete data_temp4;
      r_ans = appendOutput(rmat, R_OutputValues);;
      UNPROTECT(1);
    }
  }
  {
    if(data_temp5 != NULL) {
      R_len_t m = data_temp5->m();
      R_len_t n = data_temp5->n();
      R_len_t nzmax = data_temp5->nzmax();
      SEXP indptr, indices, vdata, dims, output;
      PROTECT(indptr = Rf_allocVector(INTSXP,n + 1));
      PROTECT(indices = Rf_allocVector(INTSXP,nzmax));
      PROTECT(vdata = Rf_allocVector(LGLSXP,nzmax));
      PROTECT(dims = Rf_allocVector(VECSXP,2));
      SET_VECTOR_ELT(dims,0,Rf_ScalarInteger(m));
      SET_VECTOR_ELT(dims,1,Rf_ScalarInteger(n));
      
      bool *xdata = data_temp5->v();
      memcpy(LOGICAL(vdata),xdata,nzmax * sizeof(bool));
      int *pB = data_temp5->pB();
      int *r = data_temp5->r();
      memcpy(INTEGER(indices),r,nzmax * sizeof(int));
      memcpy(INTEGER(indptr),pB,(n + 1) * sizeof(int));
      delete data_temp5;
      PROTECT(output = Rf_allocVector(VECSXP,4));
      SET_VECTOR_ELT(output,0,indptr);
      SET_VECTOR_ELT(output,1,indices);
      SET_VECTOR_ELT(output,2,vdata);
      SET_VECTOR_ELT(output,3,dims);
      r_ans = appendOutput(output, R_OutputValues);;
      UNPROTECT(5);
    }
  }
  {
    //# test argout
    if(data_temp6 != NULL) {
      R_len_t n = data_temp6->n();
      int *data = data_temp6->rawX();
      SEXP rmat;
      PROTECT(rmat = Rf_allocMatrix(INTSXP,1,n));
      if (!rmat) {
        Rf_error("Cannot alloc R matrix for arg %d",6); return R_NilValue;
      };
      int *rdata = (int *)INTEGER(rmat);
      memcpy(rdata,data,n * sizeof(int));
      delete data_temp6;
      r_ans = appendOutput(rmat, R_OutputValues);;
      UNPROTECT(1);
    }
  }
  {
    //# test argout
    if(data_temp7 != NULL) {
      R_len_t n = data_temp7->n();
      int *data = data_temp7->rawX();
      SEXP rmat;
      PROTECT(rmat = Rf_allocMatrix(INTSXP,1,n));
      if (!rmat) {
        Rf_error("Cannot alloc R matrix for arg %d",7); return R_NilValue;
      };
      int *rdata = (int *)INTEGER(rmat);
      memcpy(rdata,data,n * sizeof(int));
      delete data_temp7;
      r_ans = appendOutput(rmat, R_OutputValues);;
      UNPROTECT(1);
    }
  }
  {
    del_gstruct(arg1);
  }
  
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


SWIGEXPORT SEXP
R_swig_im2col_sliding ( SEXP A, SEXP B, SEXP m, SEXP n, SEXP RGB)
{
  Matrix< double > *arg1 = (Matrix< double > *) 0 ;
  Matrix< double > *arg2 = (Matrix< double > *) 0 ;
  int arg3 ;
  int arg4 ;
  bool arg5 ;
  unsigned int r_nprotect = 0;
  SEXP r_ans = R_NilValue ;
  VMAXTYPE r_vmax = vmaxget() ;
  
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=A;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",1); return R_NilValue;
      };
    }
    arg1 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  {
    /*@SWIG:R_typemaps.i,113,map_matrix@*/
    SEXP rmat=B;
    SEXP dims = Rf_getAttrib(rmat,Rf_install("dim"));
    if (TYPEOF(rmat) != REALSXP || LENGTH(dims) != 2)
    {
      /*SG_ERROR("Expected Double Vector as argument %d\n", m_rhs_counter);*/
      {
        Rf_error("Expected double dense matrix as argument %d",2); return R_NilValue;
      };
    }
    arg2 = new Matrix<double> ((double *)REAL(rmat),Rf_nrows(rmat),Rf_ncols(rmat));
    
    /*@SWIG@*/
  }
  arg3 = static_cast< int >(INTEGER(m)[0]);
  arg4 = static_cast< int >(INTEGER(n)[0]);
  arg5 = LOGICAL(RGB)[0] ? true : false;
  try {
    _im2col_sliding< double >(arg1,arg2,arg3,arg4,arg5);
  }
  catch(char const *_e) {
    Rf_error("Runtime Error %s",_e);
    return R_NilValue;
    
  }
  
  r_ans = R_NilValue;
  {
    delete arg1;
  }
  {
    delete arg2;
  }
  
  
  
  vmaxset(r_vmax);
  if(r_nprotect)  Rf_unprotect(r_nprotect);
  
  return r_ans;
}


#ifdef __cplusplus
}
#endif

/* -------- TYPE CONVERSION AND EQUIVALENCE RULES (BEGIN) -------- */

static swig_type_info _swigt__p_MatrixT_bool_t = {"_p_MatrixT_bool_t", "Matrix< bool > *", 0, 0, (void*)0, 0};
static swig_type_info _swigt__p_char = {"_p_char", "char *", 0, 0, (void*)0, 0};
static swig_type_info _swigt__p_int = {"_p_int", "int *", 0, 0, (void*)0, 0};
static swig_type_info _swigt__p_p_SpMatrixT_bool_t = {"_p_p_SpMatrixT_bool_t", "SpMatrix< bool > **", 0, 0, (void*)0, 0};
static swig_type_info _swigt__p_p_SpMatrixT_double_t = {"_p_p_SpMatrixT_double_t", "SpMatrix< double > **", 0, 0, (void*)0, 0};
static swig_type_info _swigt__p_p_int = {"_p_p_int", "int **", 0, 0, (void*)0, 0};
static swig_type_info _swigt__p_std__vectorT_int_t = {"_p_std__vectorT_int_t", "std::vector< int > *", 0, 0, (void*)0, 0};
static swig_type_info _swigt__p_std__vectorT_std__string_t = {"_p_std__vectorT_std__string_t", "std::vector< std::string > *", 0, 0, (void*)0, 0};
static swig_type_info _swigt__p_string = {"_p_string", "string *", 0, 0, (void*)0, 0};

static swig_type_info *swig_type_initial[] = {
  &_swigt__p_MatrixT_bool_t,
  &_swigt__p_char,
  &_swigt__p_int,
  &_swigt__p_p_SpMatrixT_bool_t,
  &_swigt__p_p_SpMatrixT_double_t,
  &_swigt__p_p_int,
  &_swigt__p_std__vectorT_int_t,
  &_swigt__p_std__vectorT_std__string_t,
  &_swigt__p_string,
};

static swig_cast_info _swigc__p_MatrixT_bool_t[] = {  {&_swigt__p_MatrixT_bool_t, 0, 0, 0},{0, 0, 0, 0}};
static swig_cast_info _swigc__p_char[] = {  {&_swigt__p_char, 0, 0, 0},{0, 0, 0, 0}};
static swig_cast_info _swigc__p_int[] = {  {&_swigt__p_int, 0, 0, 0},{0, 0, 0, 0}};
static swig_cast_info _swigc__p_p_SpMatrixT_bool_t[] = {  {&_swigt__p_p_SpMatrixT_bool_t, 0, 0, 0},{0, 0, 0, 0}};
static swig_cast_info _swigc__p_p_SpMatrixT_double_t[] = {  {&_swigt__p_p_SpMatrixT_double_t, 0, 0, 0},{0, 0, 0, 0}};
static swig_cast_info _swigc__p_p_int[] = {  {&_swigt__p_p_int, 0, 0, 0},{0, 0, 0, 0}};
static swig_cast_info _swigc__p_std__vectorT_int_t[] = {  {&_swigt__p_std__vectorT_int_t, 0, 0, 0},{0, 0, 0, 0}};
static swig_cast_info _swigc__p_std__vectorT_std__string_t[] = {  {&_swigt__p_std__vectorT_std__string_t, 0, 0, 0},{0, 0, 0, 0}};
static swig_cast_info _swigc__p_string[] = {  {&_swigt__p_string, 0, 0, 0},{0, 0, 0, 0}};

static swig_cast_info *swig_cast_initial[] = {
  _swigc__p_MatrixT_bool_t,
  _swigc__p_char,
  _swigc__p_int,
  _swigc__p_p_SpMatrixT_bool_t,
  _swigc__p_p_SpMatrixT_double_t,
  _swigc__p_p_int,
  _swigc__p_std__vectorT_int_t,
  _swigc__p_std__vectorT_std__string_t,
  _swigc__p_string,
};


/* -------- TYPE CONVERSION AND EQUIVALENCE RULES (END) -------- */


/* -----------------------------------------------------------------------------
 * Type initialization:
 * This problem is tough by the requirement that no dynamic
 * memory is used. Also, since swig_type_info structures store pointers to
 * swig_cast_info structures and swig_cast_info structures store pointers back
 * to swig_type_info structures, we need some lookup code at initialization.
 * The idea is that swig generates all the structures that are needed.
 * The runtime then collects these partially filled structures.
 * The SWIG_InitializeModule function takes these initial arrays out of
 * swig_module, and does all the lookup, filling in the swig_module.types
 * array with the correct data and linking the correct swig_cast_info
 * structures together.
 *
 * The generated swig_type_info structures are assigned statically to an initial
 * array. We just loop through that array, and handle each type individually.
 * First we lookup if this type has been already loaded, and if so, use the
 * loaded structure instead of the generated one. Then we have to fill in the
 * cast linked list. The cast data is initially stored in something like a
 * two-dimensional array. Each row corresponds to a type (there are the same
 * number of rows as there are in the swig_type_initial array). Each entry in
 * a column is one of the swig_cast_info structures for that type.
 * The cast_initial array is actually an array of arrays, because each row has
 * a variable number of columns. So to actually build the cast linked list,
 * we find the array of casts associated with the type, and loop through it
 * adding the casts to the list. The one last trick we need to do is making
 * sure the type pointer in the swig_cast_info struct is correct.
 *
 * First off, we lookup the cast->type name to see if it is already loaded.
 * There are three cases to handle:
 *  1) If the cast->type has already been loaded AND the type we are adding
 *     casting info to has not been loaded (it is in this module), THEN we
 *     replace the cast->type pointer with the type pointer that has already
 *     been loaded.
 *  2) If BOTH types (the one we are adding casting info to, and the
 *     cast->type) are loaded, THEN the cast info has already been loaded by
 *     the previous module so we just ignore it.
 *  3) Finally, if cast->type has not already been loaded, then we add that
 *     swig_cast_info to the linked list (because the cast->type) pointer will
 *     be correct.
 * ----------------------------------------------------------------------------- */

#ifdef __cplusplus
extern "C" {
#if 0
} /* c-mode */
#endif
#endif

#if 0
#define SWIGRUNTIME_DEBUG
#endif


SWIGRUNTIME void
SWIG_InitializeModule(void *clientdata) {
  size_t i;
  swig_module_info *module_head, *iter;
  int init;

  /* check to see if the circular list has been setup, if not, set it up */
  if (swig_module.next==0) {
    /* Initialize the swig_module */
    swig_module.type_initial = swig_type_initial;
    swig_module.cast_initial = swig_cast_initial;
    swig_module.next = &swig_module;
    init = 1;
  } else {
    init = 0;
  }

  /* Try and load any already created modules */
  module_head = SWIG_GetModule(clientdata);
  if (!module_head) {
    /* This is the first module loaded for this interpreter */
    /* so set the swig module into the interpreter */
    SWIG_SetModule(clientdata, &swig_module);
  } else {
    /* the interpreter has loaded a SWIG module, but has it loaded this one? */
    iter=module_head;
    do {
      if (iter==&swig_module) {
        /* Our module is already in the list, so there's nothing more to do. */
        return;
      }
      iter=iter->next;
    } while (iter!= module_head);

    /* otherwise we must add our module into the list */
    swig_module.next = module_head->next;
    module_head->next = &swig_module;
  }

  /* When multiple interpreters are used, a module could have already been initialized in
     a different interpreter, but not yet have a pointer in this interpreter.
     In this case, we do not want to continue adding types... everything should be
     set up already */
  if (init == 0) return;

  /* Now work on filling in swig_module.types */
#ifdef SWIGRUNTIME_DEBUG
  printf("SWIG_InitializeModule: size %d\n", swig_module.size);
#endif
  for (i = 0; i < swig_module.size; ++i) {
    swig_type_info *type = 0;
    swig_type_info *ret;
    swig_cast_info *cast;

#ifdef SWIGRUNTIME_DEBUG
    printf("SWIG_InitializeModule: type %d %s\n", i, swig_module.type_initial[i]->name);
#endif

    /* if there is another module already loaded */
    if (swig_module.next != &swig_module) {
      type = SWIG_MangledTypeQueryModule(swig_module.next, &swig_module, swig_module.type_initial[i]->name);
    }
    if (type) {
      /* Overwrite clientdata field */
#ifdef SWIGRUNTIME_DEBUG
      printf("SWIG_InitializeModule: found type %s\n", type->name);
#endif
      if (swig_module.type_initial[i]->clientdata) {
	type->clientdata = swig_module.type_initial[i]->clientdata;
#ifdef SWIGRUNTIME_DEBUG
      printf("SWIG_InitializeModule: found and overwrite type %s \n", type->name);
#endif
      }
    } else {
      type = swig_module.type_initial[i];
    }

    /* Insert casting types */
    cast = swig_module.cast_initial[i];
    while (cast->type) {

      /* Don't need to add information already in the list */
      ret = 0;
#ifdef SWIGRUNTIME_DEBUG
      printf("SWIG_InitializeModule: look cast %s\n", cast->type->name);
#endif
      if (swig_module.next != &swig_module) {
        ret = SWIG_MangledTypeQueryModule(swig_module.next, &swig_module, cast->type->name);
#ifdef SWIGRUNTIME_DEBUG
	if (ret) printf("SWIG_InitializeModule: found cast %s\n", ret->name);
#endif
      }
      if (ret) {
	if (type == swig_module.type_initial[i]) {
#ifdef SWIGRUNTIME_DEBUG
	  printf("SWIG_InitializeModule: skip old type %s\n", ret->name);
#endif
	  cast->type = ret;
	  ret = 0;
	} else {
	  /* Check for casting already in the list */
	  swig_cast_info *ocast = SWIG_TypeCheck(ret->name, type);
#ifdef SWIGRUNTIME_DEBUG
	  if (ocast) printf("SWIG_InitializeModule: skip old cast %s\n", ret->name);
#endif
	  if (!ocast) ret = 0;
	}
      }

      if (!ret) {
#ifdef SWIGRUNTIME_DEBUG
	printf("SWIG_InitializeModule: adding cast %s\n", cast->type->name);
#endif
        if (type->cast) {
          type->cast->prev = cast;
          cast->next = type->cast;
        }
        type->cast = cast;
      }
      cast++;
    }
    /* Set entry in modules->types array equal to the type */
    swig_module.types[i] = type;
  }
  swig_module.types[i] = 0;

#ifdef SWIGRUNTIME_DEBUG
  printf("**** SWIG_InitializeModule: Cast List ******\n");
  for (i = 0; i < swig_module.size; ++i) {
    int j = 0;
    swig_cast_info *cast = swig_module.cast_initial[i];
    printf("SWIG_InitializeModule: type %d %s\n", i, swig_module.type_initial[i]->name);
    while (cast->type) {
      printf("SWIG_InitializeModule: cast type %s\n", cast->type->name);
      cast++;
      ++j;
    }
  printf("---- Total casts: %d\n",j);
  }
  printf("**** SWIG_InitializeModule: Cast List ******\n");
#endif
}

/* This function will propagate the clientdata field of type to
* any new swig_type_info structures that have been added into the list
* of equivalent types.  It is like calling
* SWIG_TypeClientData(type, clientdata) a second time.
*/
SWIGRUNTIME void
SWIG_PropagateClientData(void) {
  size_t i;
  swig_cast_info *equiv;
  static int init_run = 0;

  if (init_run) return;
  init_run = 1;

  for (i = 0; i < swig_module.size; i++) {
    if (swig_module.types[i]->clientdata) {
      equiv = swig_module.types[i]->cast;
      while (equiv) {
        if (!equiv->converter) {
          if (equiv->type && !equiv->type->clientdata)
            SWIG_TypeClientData(equiv->type, swig_module.types[i]->clientdata);
        }
        equiv = equiv->next;
      }
    }
  }
}

#ifdef __cplusplus
#if 0
{ /* c-mode */
#endif
}
#endif


SWIGEXPORT void SWIG_init(void) {

}
#ifdef __cplusplus
extern "C" {
#endif

#include <R_ext/Rdynload.h>

#ifdef __cplusplus
}
#endif

SWIGINTERN R_CallMethodDef CallEntries[] = {
   {"R_swig_invSym", (DL_FUNC) &R_swig_invSym, 1},
   {"R_swig_lassoQq", (DL_FUNC) &R_swig_lassoQq, 14},
   {"R_swig_alltrainDL", (DL_FUNC) &R_swig_alltrainDL, 41},
   {"R_swig_archetypalAnalysis", (DL_FUNC) &R_swig_archetypalAnalysis, 9},
   {"R_swig_lassoD", (DL_FUNC) &R_swig_lassoD, 13},
   {"R_swig_lassoMask", (DL_FUNC) &R_swig_lassoMask, 10},
   {"R_swig_AAt", (DL_FUNC) &R_swig_AAt, 2},
   {"R_swig_archetypalAnalysisInit", (DL_FUNC) &R_swig_archetypalAnalysisInit, 8},
   {"R_swig_omp", (DL_FUNC) &R_swig_omp, 10},
   {"R_swig_normalize", (DL_FUNC) &R_swig_normalize, 1},
   {"R_swig_im2col_sliding", (DL_FUNC) &R_swig_im2col_sliding, 5},
   {"R_swig_proximalTree", (DL_FUNC) &R_swig_proximalTree, 19},
   {"R_swig_applyBayerPattern", (DL_FUNC) &R_swig_applyBayerPattern, 2},
   {"R_swig_fistaGraph", (DL_FUNC) &R_swig_fistaGraph, 42},
   {"R_swig_decompSimplex", (DL_FUNC) &R_swig_decompSimplex, 4},
   {"R_swig_somp", (DL_FUNC) &R_swig_somp, 6},
   {"R_swig_parse_line", (DL_FUNC) &R_swig_parse_line, 3},
   {"R_swig_proximalFlat", (DL_FUNC) &R_swig_proximalFlat, 16},
   {"R_swig_readGroupStruct", (DL_FUNC) &R_swig_readGroupStruct, 1},
   {"R_swig_graphOfGroupStruct", (DL_FUNC) &R_swig_graphOfGroupStruct, 1},
   {"R_swig_treeOfGroupStruct", (DL_FUNC) &R_swig_treeOfGroupStruct, 2},
   {"R_swig_groupStructOfString", (DL_FUNC) &R_swig_groupStructOfString, 1},
   {"R_swig_m_ones", (DL_FUNC) &R_swig_m_ones, 1},
   {"R_swig_simpleGroupTree", (DL_FUNC) &R_swig_simpleGroupTree, 2},
   {"R_swig_XAt", (DL_FUNC) &R_swig_XAt, 3},
   {"R_swig_cd", (DL_FUNC) &R_swig_cd, 8},
   {"R_swig_fistaTree", (DL_FUNC) &R_swig_fistaTree, 43},
   {"R_swig_ompMask", (DL_FUNC) &R_swig_ompMask, 11},
   {"R_swig_intlist", (DL_FUNC) &R_swig_intlist, 1},
   {"R_swig_sort", (DL_FUNC) &R_swig_sort, 2},
   {"R_swig_l1L2BCD", (DL_FUNC) &R_swig_l1L2BCD, 9},
   {"R_swig_lassoWeighted", (DL_FUNC) &R_swig_lassoWeighted, 9},
   {"R_swig_conjugateGradient", (DL_FUNC) &R_swig_conjugateGradient, 5},
   {"R_swig_fistaFlat", (DL_FUNC) &R_swig_fistaFlat, 40},
   {"R_swig_skip_space", (DL_FUNC) &R_swig_skip_space, 1},
   {"R_swig_mult", (DL_FUNC) &R_swig_mult, 7},
   {"R_swig_proximalGraph", (DL_FUNC) &R_swig_proximalGraph, 18},
   {"R_swig_sparseProject", (DL_FUNC) &R_swig_sparseProject, 9},
   {NULL, NULL, 0}
};

extern "C" SWIGEXPORT void R_init_spams(DllInfo *dll) {
    R_registerRoutines(dll, NULL, CallEntries, NULL, NULL);


SWIG_init();
SWIG_InitializeModule(0);


}

